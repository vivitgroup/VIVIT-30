export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {auth} from "@/lib/auth";
import {db,auditLogs,sql} from "@/lib/db";
import {executeVivitoAction,VivitoActionError,type VivitoActionArgs} from "@/lib/vivito/executor";
import {executeVivitoExtendedAction,isVivitoExtendedAction} from "@/lib/vivito/executor-extended";
import {executeVivitoOperatorAction,isVivitoOperatorAction} from "@/lib/vivito/executor-operator";
import {VIVITO_ACTION_CATALOG,type VivitoActionOp} from "@/lib/vivito/action-engine";
import {buildVivitoDryRun,decideVivitoApproval} from "@/lib/vivito/approval-policy";
import {connectionAccessToken} from "@/lib/ad-oauth";
import {mutateExternalCampaign} from "@/lib/vivito/ad-platform-writes";
import {executeVivitoPlanRuntime} from "@/lib/vivito/plan-runtime";

const clean=(v:unknown,n=120)=>String(v??"").trim().slice(0,n);
const noStore={"Cache-Control":"private, no-store"};
type UnknownRecord=Record<string,unknown>;
type ActionValue=string|number|boolean|null|Date|ActionValue[]|{[key:string]:ActionValue|undefined};
type VivitoExecutionResult={success:boolean;action?:string;entityId?:string|null;message?:string;[key:string]:unknown};
type CampaignConnectionRow={id:string;platform:string;external_id:string|null;connection_id:string|null;ad_account_id:string|null;access_token_encrypted:string|null;refresh_token_encrypted:string|null;token_expires_at:string|Date|null;connection_platform:string|null;currency:string|null};
type AuditPayloadRow={id?:string;action?:string;new_values:string|null};
type PlanStep={op:VivitoActionOp;args:VivitoActionArgs;summary:string};

const asRecord=(value:unknown):UnknownRecord=>value&&typeof value==="object"&&!Array.isArray(value)?Object.fromEntries(Object.entries(value)):{};
function toActionValue(value:unknown):ActionValue{if(value===null)return null;if(typeof value==="string"||typeof value==="number"||typeof value==="boolean")return value;if(value instanceof Date)return value;if(Array.isArray(value))return value.map(toActionValue);if(value&&typeof value==="object"){const out:{[key:string]:ActionValue|undefined}={};for(const [key,item] of Object.entries(value))out[key]=item===undefined?undefined:toActionValue(item);return out}return String(value??"")}
const toActionArgs=(value:unknown):VivitoActionArgs=>{const record=asRecord(value),out:VivitoActionArgs={};for(const [key,item] of Object.entries(record))out[key]=item===undefined?undefined:toActionValue(item);return out};
const parseRecord=(value:unknown):UnknownRecord=>{try{return typeof value==="string"?asRecord(JSON.parse(value)):asRecord(value)}catch{return{}}};
const isVivitoActionOp=(value:string):value is VivitoActionOp=>Object.prototype.hasOwnProperty.call(VIVITO_ACTION_CATALOG,value);

async function execute(op:VivitoActionOp,args:VivitoActionArgs,role:string,userId:string,workspaceId:string):Promise<VivitoExecutionResult>{if(isVivitoOperatorAction(op))return executeVivitoOperatorAction(op,args,role,userId,workspaceId);if(isVivitoExtendedAction(op))return executeVivitoExtendedAction(op,args,role,userId,workspaceId);return executeVivitoAction(op,args,role,userId,workspaceId)}
async function applyExternalCampaignWrite(op:VivitoActionOp,args:VivitoActionArgs,result:VivitoExecutionResult,userId:string,workspaceId:string){if(!workspaceId)throw new VivitoActionError("Workspace unavailable.",403);
 if(op!=="update_campaign")return null;const hasProviderField=args.status!==undefined||args.dailyBudget!==undefined||args.lifetimeBudget!==undefined;if(!hasProviderField)return{attempted:false,reason:"Only VIVIT optimization targets were changed; no provider campaign field required mutation."};const id=String(result.entityId||"");if(!id)throw new VivitoActionError("Campaign control was updated locally but the external campaign identifier could not be resolved.",502,{localApplied:true});const [row]=Array.from(await db.execute<CampaignConnectionRow>(sql`select c.id,c.platform,c.external_id,c.connection_id,x.ad_account_id,x.access_token_encrypted,x.refresh_token_encrypted,x.token_expires_at,x.platform connection_platform,(select currency from workspaces where id=${workspaceId} limit 1) currency from ad_campaigns c left join ad_platform_connections x on x.id=c.connection_id and x.workspace_id=${workspaceId} where c.id=${id} and c.workspace_id=${workspaceId} limit 1`));if(!row?.connection_id||!row.external_id)throw new VivitoActionError("Campaign desired state was saved in VIVIT, but no connected external campaign is available for mutation.",502,{localApplied:true,externalApplied:false});let token="";try{token=await connectionAccessToken({id:row.connection_id,accessTokenEncrypted:row.access_token_encrypted,refreshTokenEncrypted:row.refresh_token_encrypted,tokenExpiresAt:row.token_expires_at?new Date(row.token_expires_at):null,platform:row.connection_platform||row.platform,adAccountId:row.ad_account_id})}catch{}if(!token)throw new VivitoActionError("Campaign desired state was saved in VIVIT, but the ad-platform access token is unavailable.",502,{localApplied:true,externalApplied:false});try{const external=await mutateExternalCampaign({platform:String(row.platform||row.connection_platform),campaignId:String(row.external_id),adAccountId:String(row.ad_account_id||""),accessToken:token,status:args.status===undefined?undefined:String(args.status),dailyBudget:args.dailyBudget===undefined?undefined:Number(args.dailyBudget),lifetimeBudget:args.lifetimeBudget===undefined?undefined:Number(args.lifetimeBudget),currency:String(row.currency||"USD")});const auditValue:typeof auditLogs.$inferInsert={workspaceId,userId,action:"vivito_campaign_external_updated",entity:"ad_campaigns",entityId:id,newValues:JSON.stringify({platform:external.platform,externalCampaignId:external.campaignId,applied:external.applied,providerRequest:external.providerRequest})};await db.insert(auditLogs).values(auditValue);return{attempted:true,success:true,platform:external.platform,applied:external.applied}}catch(error){const message=error instanceof Error?error.message:"External campaign mutation failed.";const auditValue:typeof auditLogs.$inferInsert={workspaceId,userId,action:"vivito_campaign_external_failed",entity:"ad_campaigns",entityId:id,newValues:JSON.stringify({platform:row.platform,message})};await db.insert(auditLogs).values(auditValue).catch(()=>{});throw new VivitoActionError(`Campaign desired state was saved in VIVIT, but the external platform rejected the mutation: ${message}`,502,{localApplied:true,externalApplied:false,platform:row.platform})}}

async function claimExecutionReceipt(receiptId:string,workspaceId:string,userId:string,requestId:string,op:VivitoActionOp,args:VivitoActionArgs){const startedValues=JSON.stringify({requestId,op,args,state:"STARTED"});const [claimed]=Array.from(await db.execute<{id:string}>(sql`insert into audit_logs(id,workspace_id,user_id,action,entity,entity_id,new_values,created_at) values(${receiptId},${workspaceId},${userId},'vivito_action_started','vivito',${requestId},${startedValues},now()) on conflict (id) do nothing returning id`));return !!claimed}
async function readExecutionReceipt(receiptId:string,workspaceId:string,userId:string){const [prior]=Array.from(await db.execute<AuditPayloadRow>(sql`select id,action,new_values from audit_logs where id=${receiptId} and workspace_id=${workspaceId} and user_id=${userId} limit 1`));return prior||null}
async function finalizeExecutionReceipt(receiptId:string,workspaceId:string,userId:string,entityId:string,successValues:string){const updated=Array.from(await db.execute(sql`update audit_logs set action='vivito_action_executed',entity_id=${entityId},new_values=${successValues} where id=${receiptId} and workspace_id=${workspaceId} and user_id=${userId} and action='vivito_action_started' returning id`));return updated.length>0}
async function recordExecutionSuccess(workspaceId:string,userId:string,entityId:string,successValues:string){const auditValue:typeof auditLogs.$inferInsert={workspaceId,userId,action:"vivito_action_executed",entity:"vivito",entityId,newValues:successValues};await db.insert(auditLogs).values(auditValue)}
async function recordExecutionFailure(receiptId:string,workspaceId:string,userId:string,failureId:string,requestId:string,op:VivitoActionOp,args:VivitoActionArgs,message:string,status:number,details:unknown){const failureValues=JSON.stringify({requestId:requestId||null,op,args,message,status,details});if(receiptId){await db.execute(sql`update audit_logs set action='vivito_action_failed',entity_id=${failureId},new_values=${failureValues} where id=${receiptId} and workspace_id=${workspaceId} and user_id=${userId} and action='vivito_action_started'`).catch(()=>{});return}const auditValue:typeof auditLogs.$inferInsert={id:failureId,workspaceId,userId,action:"vivito_action_failed",entity:"vivito",entityId:requestId||failureId,newValues:failureValues};await db.insert(auditLogs).values(auditValue).catch(()=>{})}

export async function POST(req:NextRequest){
 const session=await auth();if(!session?.user)return NextResponse.json({error:"Unauthorized"},{status:401});
 const rawBody:unknown=await req.json().catch(()=>null);if(!rawBody||typeof rawBody!=="object"||Array.isArray(rawBody))return NextResponse.json({error:"Invalid request."},{status:400});let body=asRecord(rawBody);
 const role=String(session.user.role||""),userId=String(session.user.id||""),workspaceId=String(session.user.workspaceId||"");if(!workspaceId)return NextResponse.json({error:"Workspace unavailable"},{status:403,headers:noStore});
 if(body.retryOf){const retryId=clean(body.retryOf,120);const owner=role==="SUPER_ADMIN"?sql``:sql`and user_id=${userId}`;const [prior]=Array.from(await db.execute<AuditPayloadRow>(sql`select new_values from audit_logs where workspace_id=${workspaceId} and id=${retryId} and action='vivito_action_failed' ${owner} limit 1`));if(!prior)return NextResponse.json({error:"Retry source was not found or is not accessible."},{status:404,headers:noStore});const saved=parseRecord(prior.new_values);body={...body,op:saved.op,args:toActionArgs(saved.args),requestId:`retry:${retryId}`};}
 const requestId=clean(body.requestId,100),rawPlan=Array.isArray(body.plan)?body.plan.slice(0,8):null;
 if(rawPlan?.length){
  if(rawPlan.length<2)return NextResponse.json({error:"Multi-step execution requires at least two steps."},{status:400,headers:noStore});
  const normalized:PlanStep[]=rawPlan.flatMap(value=>{const step=asRecord(value),opRaw=clean(step.op,60);return isVivitoActionOp(opRaw)?[{op:opRaw,args:toActionArgs(step.args),summary:clean(step.summary,500)}]:[]});
  if(normalized.length!==rawPlan.length)return NextResponse.json({error:"One or more VIVITO plan steps are unsupported."},{status:400,headers:noStore});
  const decisions=normalized.map(s=>({op:s.op,...buildVivitoDryRun(s.op,s.args,role)}));
  if(body.dryRun===true)return NextResponse.json({success:true,dryRun:true,plan:true,steps:decisions,ready:decisions.every(d=>d.ready),willWrite:false},{headers:noStore});
  if(decisions.some(d=>d.approval.mode==="BLOCK"))return NextResponse.json({error:"One or more VIVITO plan steps are not authorized for your role.",steps:decisions},{status:403,headers:noStore});
  if(decisions.some(d=>d.missingFields.length))return NextResponse.json({error:"One or more VIVITO plan steps are missing required fields.",steps:decisions},{status:400,headers:noStore});
  const requiresConfirmation=decisions.some(d=>d.approval.requiresConfirmation);if(requiresConfirmation&&body.confirm!==true)return NextResponse.json({error:"Explicit confirmation is required before VIVITO executes this plan.",steps:decisions},{status:409,headers:noStore});
  const rootId=requestId||crypto.randomUUID();
  const execution=await executeVivitoPlanRuntime({steps:normalized,decisions,role,userId,workspaceId,requestId:rootId,executeStep:(op,args,stepRole,stepUserId)=>execute(op,toActionArgs(args),stepRole,stepUserId,workspaceId),applyExternal:(op,args,result,stepUserId)=>applyExternalCampaignWrite(op,toActionArgs(args),result,stepUserId,workspaceId)});
  if(execution.success===false)return NextResponse.json({success:false,partial:execution.partial,requestId:execution.requestId,completedSteps:execution.completedSteps,stoppedAt:execution.stoppedAt,error:execution.error,details:execution.details,duplicateSteps:execution.duplicateSteps},{status:execution.status,headers:noStore});
  return NextResponse.json(execution,{headers:noStore})
 }
 const opRaw=clean(body.op,60);if(!isVivitoActionOp(opRaw))return NextResponse.json({error:"Unsupported VIVITO action."},{status:400,headers:noStore});const op=opRaw;
 const args=toActionArgs(body.args),dry=buildVivitoDryRun(op,args,role);if(body.dryRun===true)return NextResponse.json({success:true,...dry},{headers:noStore});
 if(dry.approval.mode==="BLOCK")return NextResponse.json({error:dry.approval.reason,approval:dry.approval},{status:403,headers:noStore});if(dry.missingFields.length)return NextResponse.json({error:"Required action fields are missing.",missingFields:dry.missingFields,approval:dry.approval},{status:400,headers:noStore});if(dry.approval.requiresConfirmation&&body.confirm!==true)return NextResponse.json({error:"Explicit confirmation is required before VIVITO writes to the ERP.",approval:dry.approval},{status:409,headers:noStore});
 let receiptId="";
 if(requestId){
  receiptId=`vivito:req:${workspaceId}:${userId}:${requestId}`;
  const claimed=await claimExecutionReceipt(receiptId,workspaceId,userId,requestId,op,args);
  if(!claimed){
   const prior=await readExecutionReceipt(receiptId,workspaceId,userId);
   const previous=parseRecord(prior?.new_values);
   if(prior?.action==="vivito_action_executed")return NextResponse.json({success:true,duplicate:true,action:op,result:previous.result||null},{headers:noStore});
   if(prior?.action==="vivito_action_failed")return NextResponse.json({error:previous.message||"The prior execution failed.",failureId:receiptId,requestId,duplicate:true,retryWith:"retryOf"},{status:Number(previous.status||409),headers:noStore});
   return NextResponse.json({error:"A prior request with this requestId is still in progress or its outcome is uncertain. VIVITO will not execute it twice.",requestId,duplicate:true},{status:409,headers:noStore});
  }
 }
 try{const result=await execute(op,args,role,userId,workspaceId),external=await applyExternalCampaignWrite(op,args,result,userId,workspaceId),combined=external?{...result,external}:result,entityId=String(result.entityId||requestId||crypto.randomUUID()),successValues=JSON.stringify({requestId:requestId||null,op,result:combined,approval:decideVivitoApproval(op,role)});if(receiptId){const finalized=await finalizeExecutionReceipt(receiptId,workspaceId,userId,entityId,successValues);if(!finalized)throw new VivitoActionError("VIVITO completed the action but could not finalize its execution receipt.",500,{outcomeUncertain:true,requestId})}else await recordExecutionSuccess(workspaceId,userId,entityId,successValues);return NextResponse.json({success:true,action:op,result:combined},{headers:noStore})}catch(error){const status=error instanceof VivitoActionError?error.status:500,message=error instanceof Error?error.message:"VIVITO could not execute the action safely.",failureId=receiptId||crypto.randomUUID();await recordExecutionFailure(receiptId,workspaceId,userId,failureId,requestId,op,args,message,status,error instanceof VivitoActionError?error.details||null:null);if(error instanceof VivitoActionError)return NextResponse.json({error:error.message,details:error.details||null,failureId},{status:error.status,headers:noStore});console.error("VIVITO action execution failed",error);return NextResponse.json({error:"VIVITO could not execute the action safely.",failureId},{status:500,headers:noStore})}
}