export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {auth} from "@/lib/auth";
import {db,sql} from "@/lib/db";
const W="default",ar=(s:string)=>/[\u0600-\u06ff]/.test(s),n=(v:any)=>Number(v||0),cairoDay=(d:Date)=>new Intl.DateTimeFormat("en-CA",{timeZone:"Africa/Cairo",year:"numeric",month:"2-digit",day:"2-digit"}).format(d),dateLabel=(d:Date,a:boolean)=>new Intl.DateTimeFormat(a?"ar-EG":"en-GB",{timeZone:"Africa/Cairo",day:"2-digit",month:"short",year:"numeric"}).format(d);
const ANTHROPIC_URL="https://api.anthropic.com/v1/messages";
async function callClaude(prompt:string,system:string){if(!process.env.ANTHROPIC_API_KEY)throw new Error("provider-not-configured");const r=await fetch(ANTHROPIC_URL,{method:"POST",headers:{"Content-Type":"application/json","x-api-key":process.env.ANTHROPIC_API_KEY,"anthropic-version":"2023-06-01"},body:JSON.stringify({model:"claude-sonnet-4-20250514",max_tokens:1800,system,messages:[{role:"user",content:prompt}]})}),d=await r.json();if(!r.ok)throw new Error(d?.error?.message||"provider-failed");return d.content?.[0]?.text||""}
async function callGemini(prompt:string,system:string){if(!process.env.GEMINI_API_KEY)throw new Error("provider-not-configured");const model=process.env.GEMINI_MODEL||"gemini-2.0-flash",r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${process.env.GEMINI_API_KEY}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({systemInstruction:{parts:[{text:system}]},contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{temperature:.35,maxOutputTokens:1800}})}),d=await r.json();if(!r.ok)throw new Error(d?.error?.message||"provider-failed");return d?.candidates?.[0]?.content?.parts?.map((p:any)=>p.text).join("\n")||""}
async function generate(prompt:string,system:string){if(process.env.GEMINI_API_KEY)return callGemini(prompt,system);if(process.env.ANTHROPIC_API_KEY)return callClaude(prompt,system);throw new Error("provider-not-configured")}
async function clientScope(role:string,userId:string){if(role==="SUPER_ADMIN")return [...await db.execute(sql`select id,company_name from clients where workspace_id=${W} and is_active=true order by company_name`)];if(role==="ACCOUNT_MANAGER")return [...await db.execute(sql`select id,company_name from clients where workspace_id=${W} and is_active=true and account_manager_id=${userId} order by company_name`)];if(role==="MEDIA_BUYER")return [...await db.execute(sql`select id,company_name from clients where workspace_id=${W} and is_active=true and media_buyer_id=${userId} order by company_name`)];if(role==="CLIENT")return [...await db.execute(sql`select id,company_name from clients where workspace_id=${W} and is_active=true and user_id=${userId} limit 1`)];return[]}
async function taskContext(role:string,userId:string,ids:string[]){if(role==="CREATOR")return [...await db.execute(sql`select t.id,t.title,t.status,t.priority,t.deadline,t.client_id,c.company_name from creative_tasks t join clients c on c.id=t.client_id where t.workspace_id=${W} and t.archived_at is null and t.deleted_at is null and c.is_active=true and t.assigned_to_id=${userId} and t.status not in ('COMPLETED','REJECTED') order by t.deadline asc limit 120`)];if(ids.length)return [...await db.execute(sql`select t.id,t.title,t.status,t.priority,t.deadline,t.client_id,c.company_name from creative_tasks t join clients c on c.id=t.client_id where t.workspace_id=${W} and t.archived_at is null and t.deleted_at is null and c.is_active=true and t.client_id in (${sql.join(ids.map(id=>sql`${id}`),sql`,`)}) and t.status not in ('COMPLETED','REJECTED') order by t.deadline asc limit 120`)];return[]}
function taskAnswer(question:string,tasks:any[]){const a=ar(question),q=question.toLowerCase(),today=cairoDay(new Date()),todayTasks=tasks.filter(t=>cairoDay(new Date(t.deadline))===today),overdue=tasks.filter(t=>new Date(t.deadline).getTime()<Date.now()&&cairoDay(new Date(t.deadline))!==today),review=tasks.filter(t=>["REVIEW","APPROVED","REVISION"].includes(String(t.status))),soon=[...tasks].filter(t=>new Date(t.deadline)>=new Date()).sort((x,y)=>+new Date(x.deadline)-+new Date(y.deadline)).slice(0,10),line=(t:any)=>`• ${t.title} — ${t.company_name||"Client"} — ${dateLabel(new Date(t.deadline),a)} — ${String(t.status).replace(/_/g," ")}`;if(/today|النهارده|اليوم|انهاردة|انهارده/.test(q))return todayTasks.length?(a?`تاسكات النهارده (${todayTasks.length}):\n${todayTasks.map(line).join("\n")}`:`Tasks due today (${todayTasks.length}):\n${todayTasks.map(line).join("\n")}`):(a?"مفيش تاسكات ديدلاينها النهارده.":"No tasks are due today.");if(/overdue|late|متأخر|متاخر|فات/.test(q))return overdue.length?(a?`التاسكات المتأخرة (${overdue.length}):\n${overdue.slice(0,12).map(line).join("\n")}`:`Overdue tasks (${overdue.length}):\n${overdue.slice(0,12).map(line).join("\n")}`):(a?"مفيش تاسكات متأخرة حاليًا.":"There are no overdue tasks right now.");if(/review|approve|approval|ريفيو|مراجعة|موافق/.test(q))return review.length?(a?`الموجود في الـReview / Approval (${review.length}):\n${review.slice(0,12).map(line).join("\n")}`:`Review / approval queue (${review.length}):\n${review.slice(0,12).map(line).join("\n")}`):(a?"مفيش حاجة في الـReview حاليًا.":"Nothing is currently in review.");return tasks.length?(a?`عندك ${tasks.length} تاسك نشطة. أقرب الديدلاينز:\n${soon.map(line).join("\n")}`:`You have ${tasks.length} active tasks. Nearest deadlines:\n${soon.map(line).join("\n")}`):(a?"مفيش تاسكات نشطة حاليًا.":"There are no active tasks right now.")}
export async function POST(req:NextRequest){const session=await auth();if(!session?.user)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await req.json().catch(()=>({})),question=String(body.question||"").trim().slice(0,1200);if(!question)return NextResponse.json({error:"Ask a question first."},{status:400});const role=String((session.user as any).role||""),userId=String((session.user as any).id||""),clients=await clientScope(role,userId),ids=clients.map((c:any)=>String(c.id)),tasks=await taskContext(role,userId,ids);if(role==="CLIENT")return NextResponse.json({answer:taskAnswer(question,tasks),sources:["Creative Tasks"],mode:"tasks"},{headers:{"Cache-Control":"private, no-store"}});if(!["SUPER_ADMIN","ACCOUNT_MANAGER","MEDIA_BUYER","CREATOR"].includes(role))return NextResponse.json({answer:taskAnswer(question,tasks),sources:["Creative Tasks"],mode:"tasks"});let media:any[]=[],billing:any[]=[];if(ids.length&&["SUPER_ADMIN","ACCOUNT_MANAGER","MEDIA_BUYER"].includes(role)){media=[...await db.execute(sql`select c.company_name,ac.name campaign,ac.status,ac.reported_result_label,coalesce(sum(p.spend),0) spend,coalesce(sum(p.results),0) results,coalesce(sum(p.add_to_cart),0) atc,coalesce(sum(p.purchases),0) purchases,coalesce(sum(p.revenue),0) revenue,coalesce(sum(p.impressions),0) impressions,coalesce(sum(p.clicks),0) clicks from ad_campaigns ac join clients c on c.id=ac.client_id left join ad_performance_daily p on p.campaign_id=ac.id and p.date>=date_trunc('month',now()) and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null where ac.client_id in (${sql.join(ids.map(id=>sql`${id}`),sql`,`)}) group by c.company_name,ac.id,ac.name,ac.status,ac.reported_result_label order by spend desc limit 80`)];}if(role==="SUPER_ADMIN"&&ids.length){billing=[...await db.execute(sql`select c.company_name,p.amount_due,p.amount_paid,p.amount_remaining,p.payment_day,p.payment_status from client_payment_profiles p join clients c on c.id=p.client_id where p.client_id in (${sql.join(ids.map(id=>sql`${id}`),sql`,`)}) order by p.amount_remaining desc`)];}const overdue=tasks.filter(t=>new Date(t.deadline).getTime()<Date.now()),mediaSpend=media.reduce((s,x)=>s+n(x.spend),0),messages=media.filter(x=>String(x.reported_result_label||"").toLowerCase().includes("messag")).reduce((s,x)=>s+n(x.results),0),atc=media.reduce((s,x)=>s+n(x.atc),0),purchases=media.reduce((s,x)=>s+n(x.purchases),0),outstanding=billing.reduce((s,x)=>s+n(x.amount_remaining),0),canSeeFinance=role==="SUPER_ADMIN",context:any={role,activeClients:clients.length,activeTasks:tasks.length,overdueTasks:overdue.length,mediaSpendMTD:mediaSpend,messagesMTD:messages,addToCartMTD:atc,purchasesMTD:purchases,topTasks:tasks.slice(0,20),campaigns:media.slice(0,30)};if(canSeeFinance){context.outstandingEGP=outstanding;context.billing=billing.slice(0,20)}const system=`You are FAHD, VIVIT's operating intelligence layer. Role: ${role}. Use only the supplied ERP LIVE CONTEXT for live client, task and campaign facts. Never invent a metric. Do not expose finance unless it exists in the supplied context. If the user writes Arabic, answer Egyptian Arabic with clear English marketing terms where useful. Be concise, practical and decisive.`;const prompt=`QUESTION:\n${question}\n\nERP LIVE CONTEXT:\n${JSON.stringify(context)}`;try{const answer=await generate(prompt,system);return NextResponse.json({answer,sources:canSeeFinance?["Creative Tasks","Media Campaigns","Client Billing"]:["Creative Tasks","Media Campaigns"],mode:"advisor"},{headers:{"Cache-Control":"private, no-store"}})}catch{const base=taskAnswer(question,tasks);const extra=media.length?`\n\nLive media snapshot: ${Math.round(mediaSpend).toLocaleString("en-EG")} EGP spend MTD · ${messages} messages · ${atc} ATC · ${purchases} purchases.`:"";return NextResponse.json({answer:`${base}${extra}`,sources:["Creative Tasks","Media Campaigns"],mode:"erp-fallback"},{status:200,headers:{"Cache-Control":"private, no-store"}})}}
