export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";import {auth} from "@/lib/auth";import {db,sql} from "@/lib/db";import {generateText} from "@/lib/copilot/provider";const WS="default";
function scope(role:string,userId:string){if(role==="SUPER_ADMIN"||role==="ACCOUNTANT")return sql`true`;if(role==="ACCOUNT_MANAGER")return sql`c.account_manager_id=${userId}`;if(role==="MEDIA_BUYER")return sql`c.media_buyer_id=${userId}`;if(role==="CLIENT")return sql`c.user_id=${userId}`;return sql`false`}
async function ctx(role:string,userId:string){const sc=scope(role,userId),taskSc=role==="CREATOR"?sql`t.assigned_to_id=${userId}`:sc;
 const tasks=Array.from(await db.execute(sql`select t.id,t.client_id,t.title,t.status,t.priority,t.type,t.brief,t.deadline,t.assigned_to_id,c.company_name from creative_tasks t join clients c on c.id=t.client_id where t.workspace_id=${WS} and t.archived_at is null and c.is_active=true and ${taskSc} and t.status not in ('COMPLETED','REJECTED') order by t.deadline asc limit 80`) as any);
 const clients=["SUPER_ADMIN","ACCOUNT_MANAGER","MEDIA_BUYER"].includes(role)?Array.from(await db.execute(sql`select c.id,c.company_name,c.health_score,c.churn_risk,c.monthly_retainer,c.internal_notes,c.color_palette from clients c where c.workspace_id=${WS} and c.is_active=true and ${sc} order by c.company_name limit 60`) as any):[];
 const billing=["SUPER_ADMIN","ACCOUNT_MANAGER","ACCOUNTANT"].includes(role)?Array.from(await db.execute(sql`select c.id client_id,c.company_name,coalesce(p.amount_due,0) amount_due,coalesce(p.amount_paid,0) amount_paid,coalesce(p.amount_remaining,0) outstanding,p.payment_status,p.payment_day,p.phone,p.notes from clients c left join client_payment_profiles p on p.client_id=c.id where c.workspace_id=${WS} and c.is_active=true and ${sc} order by coalesce(p.amount_remaining,0) desc limit 60`) as any):[];
 const media=["SUPER_ADMIN","ACCOUNT_MANAGER","MEDIA_BUYER","CLIENT"].includes(role)?Array.from(await db.execute(sql`select c.id client_id,c.company_name,ac.id campaign_id,ac.name,ac.status,ac.reported_result_type,ac.reported_result_label,coalesce((ac.reported_metrics->>'spend')::numeric,0) spend,coalesce((ac.reported_metrics->>'results')::numeric,0) results,coalesce((ac.reported_metrics->>'costPerResult')::numeric,0) cost_per_result,coalesce((ac.reported_metrics->>'addToCart')::numeric,0) atc,coalesce((ac.reported_metrics->>'purchases')::numeric,0) purchases,coalesce((ac.reported_metrics->>'ctr')::numeric,0) ctr,coalesce((ac.reported_metrics->>'frequency')::numeric,0) frequency,coalesce((ac.reported_metrics->>'revenue')::numeric,0) revenue,ac.last_sync_at from ad_campaigns ac join clients c on c.id=ac.client_id where c.workspace_id=${WS} and c.is_active=true and ac.archived_at is null and ${sc} order by coalesce((ac.reported_metrics->>'spend')::numeric,0) desc limit 100`) as any):[];
 const sales=role==="SALES"||role==="SUPER_ADMIN"?Array.from(await db.execute(sql`select id,company_name,contact_person,stage,estimated_value,probability,next_follow_up,follow_up_count,expected_close,notes from sales_leads where workspace_id=${WS} and (${role==="SUPER_ADMIN"?sql`true`:sql`sales_rep_id=${userId}`}) and stage not in ('WON','LOST') order by next_follow_up asc nulls last,probability desc limit 60`) as any):[];
 const workload=role==="SUPER_ADMIN"?Array.from(await db.execute(sql`select u.id,u.name,u.role,count(t.id)::int active_tasks,count(t.id) filter(where t.deadline<now())::int overdue,count(t.id) filter(where t.deadline<now()+interval '24 hours')::int due_24h from users u left join creative_tasks t on t.assigned_to_id=u.id and t.archived_at is null and t.status not in ('COMPLETED','APPROVED','REJECTED') where u.workspace_id=${WS} and u.is_active=true and u.role in ('CREATOR','ACCOUNT_MANAGER','MEDIA_BUYER') group by u.id,u.name,u.role order by overdue desc,active_tasks desc`) as any):[];
 return{tasks,clients,billing,media,sales,workload};}
function recs(role:string,x:any){const now=Date.now(),a:any[]=[];for(const t of x.tasks){const h=(+new Date(t.deadline)-now)/36e5;if(h<0)a.push({level:"critical",type:"task",title:`Overdue: ${t.title}`,why:`${t.company_name} · deadline passed`,action:"Open task",href:`/dashboard/creative/${t.id}`,score:96});else if(h<24)a.push({level:"high",type:"task",title:`Due within 24h: ${t.title}`,why:`${t.company_name} · ${Math.max(1,Math.round(h))}h left`,action:"Open task",href:`/dashboard/creative/${t.id}`,score:86})}for(const b of x.billing){const o=Number(b.outstanding||0);if(o>0)a.push({level:o>=25000?"critical":"high",type:"finance",title:`Collect ${o.toLocaleString("en-EG")} EGP from ${b.company_name}`,why:`${b.payment_status||"DUE"} · payment day ${b.payment_day||"not set"}`,action:"Open payments",href:"/dashboard/clients/accounts-payment",score:Math.min(98,70+Math.round(o/2000))})}for(const m of x.media){const s=Number(m.spend||0),r=Number(m.results||0),c=Number(m.cost_per_result||0),f=Number(m.frequency||0);if(s>500&&r===0)a.push({level:"critical",type:"media",title:`${m.company_name}: spend with 0 ${m.reported_result_label||"results"}`,why:`${m.name} · ${s.toFixed(0)} EGP spent`,action:"Review campaign",href:"/dashboard/media/control-center",score:95});else if(f>=3&&s>500)a.push({level:"high",type:"media",title:`Possible creative fatigue · ${m.company_name}`,why:`${m.name} · frequency ${f.toFixed(2)} · cost/result ${c.toFixed(1)}`,action:"Review creative",href:"/dashboard/media/control-center",score:82})}for(const l of x.sales){if(l.next_follow_up&&+new Date(l.next_follow_up)<=now+86400000)a.push({level:"high",type:"sales",title:`Follow up · ${l.company_name}`,why:`${l.stage} · ${l.probability||0}% probability · value ${Number(l.estimated_value||0).toLocaleString("en-EG")}`,action:"Open sales",href:"/dashboard/sales",score:84})}for(const w of x.workload){if(Number(w.overdue)>0||Number(w.active_tasks)>=10)a.push({level:"high",type:"workload",title:`Workload attention · ${w.name}`,why:`${w.active_tasks} active · ${w.overdue} overdue · ${w.due_24h} due in 24h`,action:"Open creative board",href:"/dashboard/creative",score:80+Math.min(15,Number(w.overdue)*3)})}if(role==="CREATOR"&&!a.length)a.push({level:"info",type:"plan",title:"Production queue is under control",why:"No overdue or next-24h blockers detected.",action:"Open Creative",href:"/dashboard/creative",score:30});return a.sort((p,q)=>q.score-p.score).slice(0,16)}
export async function GET(){const s=await auth();if(!s?.user)return NextResponse.json({error:"Unauthorized"},{status:401});const role=String((s.user as any).role||"CLIENT"),userId=String((s.user as any).id||""),x=await ctx(role,userId),recommendations=recs(role,x);return NextResponse.json({role,recommendations,counts:{tasks:x.tasks.length,clients:x.clients.length,billing:x.billing.filter((b:any)=>Number(b.outstanding)>0).length,campaigns:x.media.length,sales:x.sales.length},freshness:{generatedAt:new Date().toISOString(),mediaLastSync:x.media.map((m:any)=>m.last_sync_at).filter(Boolean).sort().at(-1)||null}},{headers:{"Cache-Control":"private, no-store"}})}
export async function POST(req:NextRequest){const s=await auth();if(!s?.user)return NextResponse.json({error:"Unauthorized"},{status:401});const role=String((s.user as any).role||"CLIENT"),userId=String((s.user as any).id||""),b=await req.json().catch(()=>({})),question=String(b.question||"").trim().slice(0,2200),mode=String(b.mode||"ASK").toUpperCase();if(!question)return NextResponse.json({error:"Ask a question first."},{status:400});const x=await ctx(role,userId),r=recs(role,x),rules:Record<string,string>={CLIENT:"Only discuss this client's visible tasks, approvals, deadlines and media. Never expose internal notes, billing, other clients or staff data.",CREATOR:"Manage the creator's production day: prioritize assigned work, improve briefs, propose concepts, hooks, art direction, editing direction and pre-flight design QA.",ACCOUNT_MANAGER:"Manage assigned clients proactively: delivery, approvals, communication, billing signals, content gaps, strategy and next actions.",MEDIA_BUYER:"Operate as a senior media analyst: respect each campaign's semantic result type (Messages, ATC, Purchases, Leads), diagnose spend, cost/result, CTR, frequency and tracking signals. Recommend scale/hold/reduce/test but never change spend without approval.",ACCOUNTANT:"Operate as finance controller: collections, outstanding, payment timing, cash priorities and mismatches. Do not expose unrelated creative/media internals.",SALES:"Operate as sales manager: prioritize leads, next follow-ups, qualification, objections, proposals and close probability.",SUPER_ADMIN:"Operate as executive COO/CMO/CFO: connect clients, delivery, finance, media, sales and workload; find root causes, risks, opportunities and highest-impact actions."};const system=`You are VIVIT Copilot in ${mode} mode. ${rules[role]||"Help within the user's role."}\nYou are not a passive chatbot. Proactively identify what should happen next. Separate LIVE ERP FACTS from ADVICE. Never invent a number. Client internal_notes and color_palette are private working memory and may be used only for internal roles, never quoted to clients as hidden notes. For general questions, act as a senior practitioner in marketing strategy, research, content, copywriting, graphic design, art direction, video editing, social media, Meta/TikTok/Google media buying, funnels and agency operations. If the user writes Arabic, answer Egyptian Arabic. In ACT mode, propose executable actions as an 'Actions ready for approval' section; do not claim an action executed unless an action endpoint confirms it.`;const prompt=`QUESTION:\n${question}\n\nLIVE ERP CONTEXT:\n${JSON.stringify(x).slice(0,30000)}\n\nPROACTIVE SIGNALS:\n${JSON.stringify(r).slice(0,9000)}\n\nReturn a direct useful answer. When useful include: What happened → Why it matters → Recommendation → Expected impact/risk → Actions ready for approval. End with Sources used: exact ERP modules, or General marketing knowledge.`;try{const out=await generateText({system,prompt});return NextResponse.json({answer:out.text,provider:out.provider,sources:[x.tasks.length&&"Creative Tasks",x.clients.length&&"Clients / Memory",x.billing.length&&"Billing",x.media.length&&"Media",x.sales.length&&"Sales",x.workload.length&&"Team Workload"].filter(Boolean),mode,freshness:{generatedAt:new Date().toISOString(),mediaLastSync:x.media.map((m:any)=>m.last_sync_at).filter(Boolean).sort().at(-1)||null}})}catch(e:any){return NextResponse.json({answer:`Copilot AI providers are temporarily busy. Your live Today signals are still available (${r.length} recommendations).`,recommendations:r,sources:["Live ERP rules"],mode:"FALLBACK"},{status:200})}}
