export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {db,sql,notifications} from "@/lib/db";
import {buildDailyCompetitiveReport} from "@/lib/vivito/competitive-intelligence";
type DbRow=Record<string,unknown>;
type CompetitiveResult={workspaceId:string;clientId:string;ok:boolean;summary?:string;error?:string};
const rows=(v:unknown):DbRow[]=>Array.from(v as Iterable<DbRow>);
export async function GET(req:NextRequest){const bearer=req.headers.get("authorization")?.replace(/^Bearer\s+/i,""),secret=req.headers.get("x-cron-secret")||bearer||req.nextUrl.searchParams.get("secret");if(process.env.NODE_ENV==="production"&&(!process.env.CRON_SECRET||secret!==process.env.CRON_SECRET))return NextResponse.json({error:"Unauthorized"},{status:401});const clients=rows(await db.execute(sql`select distinct w.workspace_id,w.client_id,c.company_name,c.account_manager_id from competitor_watchlists w join clients c on c.id=w.client_id and c.workspace_id=w.workspace_id where w.is_active=true and c.is_active=true`)),results:CompetitiveResult[]=[];for(const c of clients){const workspaceId=String(c.workspace_id),clientId=String(c.client_id);try{const report=await buildDailyCompetitiveReport(clientId,workspaceId),admins=rows(await db.execute(sql`select id,email,name from users where workspace_id=${workspaceId} and role='SUPER_ADMIN' and is_active=true`)),targets=[...new Set([String(c.account_manager_id||""),...admins.map(a=>String(a.id))].filter(Boolean))];for(const userId of targets)await db.insert(notifications).values({userId,type:"GENERAL",priority:"normal",title:`📡 Competitive Watch — ${c.company_name}`,message:report.summary.slice(0,1800),link:`/dashboard/clients/${clientId}`}).onConflictDoNothing();if(targets.length&&process.env.RESEND_API_KEY){const emails=rows(await db.execute(sql`select email,name from users where workspace_id=${workspaceId} and id in (${sql.join(targets.map(id=>sql`${id}`),sql`,`)}) and is_active=true`));for(const u of emails){if(!u.email)continue;await fetch("https://api.resend.com/emails",{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${process.env.RESEND_API_KEY}`},body:JSON.stringify({from:process.env.EMAIL_FROM||"VIVIT ERP <noreply@viviterp.com>",to:[u.email],subject:`Competitive Intelligence Daily — ${c.company_name}`,html:`<div style="font-family:Arial,sans-serif;max-width:680px;margin:auto;padding:24px"><h2>📡 Competitive Intelligence Daily</h2><p><b>${c.company_name}</b></p><pre style="white-space:pre-wrap;font-family:Arial,sans-serif;line-height:1.55;background:#f6f7f9;padding:16px;border-radius:10px">${report.summary.replace(/[&<>]/g,(m:string)=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[m]||m))}</pre><p style="color:#777;font-size:12px">Public competitive monitoring by VIVITO. Metrics not publicly observable are marked unavailable instead of estimated.</p></div>`}),signal:AbortSignal.timeout(10000)}).catch(()=>{})}}results.push({workspaceId,clientId,ok:true,summary:report.summary})}catch(e:unknown){results.push({workspaceId,clientId,ok:false,error:e instanceof Error?e.message:String(e)})}}return NextResponse.json({ok:true,clients:results.length,results},{headers:{"Cache-Control":"no-store"}})}
