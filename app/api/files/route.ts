// @ts-nocheck
export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {auth} from "@/lib/auth";
import {db,fileDocuments,auditLogs,clients,sql} from "@/lib/db";
import {eq,and,desc,inArray,or} from "drizzle-orm";

const WORKSPACE="default",BUCKET="vivit-files",MAX_SIZE=500*1024*1024;
const base=()=>String(process.env.SUPABASE_URL||"").replace(/\/$/,"");
const headers=()=>({apikey:process.env.SUPABASE_SERVICE_KEY!,Authorization:`Bearer ${process.env.SUPABASE_SERVICE_KEY!}`});
const safeName=(name:string)=>name.normalize("NFKD").replace(/[^a-zA-Z0-9._-]/g,"-").replace(/-+/g,"-").slice(-140)||"file";
const clean=(v:any,n=255)=>String(v||"").trim().slice(0,n);

async function ensureBucket(){const payload={id:BUCKET,name:BUCKET,public:false,file_size_limit:MAX_SIZE,allowed_mime_types:null};const create=await fetch(`${base()}/storage/v1/bucket`,{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify(payload)});if(create.ok||create.status===409){await fetch(`${base()}/storage/v1/bucket/${BUCKET}`,{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({public:false,file_size_limit:MAX_SIZE,allowed_mime_types:null})}).catch(()=>null);return}try{await db.execute(sql`insert into storage.buckets (id,name,public,file_size_limit) values (${BUCKET},${BUCKET},false,${MAX_SIZE}) on conflict (id) do update set public=false,file_size_limit=${MAX_SIZE}`);return}catch(e){console.error("Storage bucket provisioning failed",e)}throw new Error("Storage bucket is unavailable")}

async function scopeFor(role:string,userId:string){const own:any=eq(fileDocuments.uploadedBy,userId),workspace=eq(fileDocuments.workspaceId,WORKSPACE);if(role==="SUPER_ADMIN")return workspace;if(role==="CLIENT"){const rows=await db.select({id:clients.id}).from(clients).where(and(eq(clients.workspaceId,WORKSPACE),eq(clients.userId,userId),eq(clients.isActive,true)));return and(workspace,rows.length?or(own,inArray(fileDocuments.clientId,rows.map(x=>x.id))):own)}if(role==="ACCOUNT_MANAGER"||role==="MEDIA_BUYER"){const rows=await db.select({id:clients.id}).from(clients).where(and(eq(clients.workspaceId,WORKSPACE),eq(clients.isActive,true),role==="ACCOUNT_MANAGER"?eq(clients.accountManagerId,userId):eq(clients.mediaBuyerId,userId)));return and(workspace,rows.length?or(own,inArray(fileDocuments.clientId,rows.map(x=>x.id))):own)}if(role==="CREATOR"){const rows=Array.from(await db.execute(sql`select t.id,t.client_id from creative_tasks t join clients c on c.id=t.client_id where t.workspace_id=${WORKSPACE} and c.workspace_id=${WORKSPACE} and t.assigned_to_id=${userId} and t.archived_at is null and c.is_active=true`)) as any[];if(!rows.length)return and(workspace,own);const taskIds=[...new Set(rows.map(x=>x.id))],clientIds=[...new Set(rows.map(x=>x.client_id).filter(Boolean))];return and(workspace,or(own,inArray(fileDocuments.taskId,taskIds),and(inArray(fileDocuments.clientId,clientIds),inArray(fileDocuments.category,["CONTENT_PLAN","STRATEGY","BRIEF","CREATIVE","SOCIAL_POST"]))))}if(role==="ACCOUNTANT")return and(workspace,or(own,inArray(fileDocuments.category,["CONTRACT","INVOICE","FINANCE","SHEET"])));return and(workspace,own)}

async function validateLinks(role:string,userId:string,clientId:string|null,taskId:string|null,category:string){if(!clientId&&!taskId)return true;if(taskId){const rows=Array.from(await db.execute(sql`select t.client_id,t.assigned_to_id,c.user_id,c.account_manager_id,c.media_buyer_id from creative_tasks t join clients c on c.id=t.client_id where t.id=${taskId} and t.workspace_id=${WORKSPACE} and c.workspace_id=${WORKSPACE} and t.archived_at is null and c.is_active=true limit 1`)) as any[];const task=rows[0];if(!task||(clientId&&task.client_id!==clientId))return false;clientId=task.client_id;if(role==="SUPER_ADMIN")return true;if(role==="CREATOR")return task.assigned_to_id===userId;if(role==="ACCOUNT_MANAGER")return task.account_manager_id===userId;if(role==="MEDIA_BUYER")return task.media_buyer_id===userId;if(role==="CLIENT")return task.user_id===userId;if(role==="ACCOUNTANT")return ["CONTRACT","INVOICE","FINANCE","SHEET"].includes(category);return false}if(!clientId)return false;const [client]=await db.select({userId:clients.userId,accountManagerId:clients.accountManagerId,mediaBuyerId:clients.mediaBuyerId}).from(clients).where(and(eq(clients.id,clientId),eq(clients.workspaceId,WORKSPACE),eq(clients.isActive,true))).limit(1);if(!client)return false;if(role==="SUPER_ADMIN")return true;if(role==="CREATOR"){const rows=Array.from(await db.execute(sql`select 1 from creative_tasks where workspace_id=${WORKSPACE} and client_id=${clientId} and assigned_to_id=${userId} and archived_at is null limit 1`));return rows.length>0}if(role==="ACCOUNT_MANAGER")return client.accountManagerId===userId;if(role==="MEDIA_BUYER")return client.mediaBuyerId===userId;if(role==="CLIENT")return client.userId===userId;if(role==="ACCOUNTANT")return ["CONTRACT","INVOICE","FINANCE","SHEET"].includes(category);return false}
async function editableFile(id:string,role:string,userId:string){const [row]=await db.select().from(fileDocuments).where(and(eq(fileDocuments.id,id),eq(fileDocuments.workspaceId,WORKSPACE))).limit(1);if(!row)return {row:null,allowed:false};return {row,allowed:role==="SUPER_ADMIN"||row.uploadedBy===userId}}
async function signRead(path:string){const r=await fetch(`${base()}/storage/v1/object/sign/${BUCKET}/${path}`,{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({expiresIn:1800})});const d=await r.json().catch(()=>({}));return d.signedURL?`${base()}/storage/v1${d.signedURL}`:null}

export async function GET(req:NextRequest){const session=await auth();if(!session?.user)return NextResponse.json({error:"Unauthorized"},{status:401});const role=String((session.user as any).role),userId=String((session.user as any).id),archived=req.nextUrl.searchParams.get("view")==="archived",taskId=clean(req.nextUrl.searchParams.get("taskId"),100),clientId=clean(req.nextUrl.searchParams.get("clientId"),100);if((taskId||clientId)&&!(await validateLinks(role,userId,clientId||null,taskId||null,"CREATIVE")))return NextResponse.json({error:"The selected client or task is archived or unavailable to you."},{status:403});const archiveCondition=archived?sql`${fileDocuments.id} in (select id from file_documents where workspace_id=${WORKSPACE} and archived_at is not null)`:sql`${fileDocuments.id} in (select id from file_documents where workspace_id=${WORKSPACE} and archived_at is null)`;const filters:any[]=[await scopeFor(role,userId),archiveCondition];if(taskId)filters.push(eq(fileDocuments.taskId,taskId));if(clientId)filters.push(eq(fileDocuments.clientId,clientId));const rows=await db.select().from(fileDocuments).where(and(...filters)).orderBy(desc(fileDocuments.createdAt)).limit(150);const files=await Promise.all(rows.map(async f=>({...f,isArchived:archived,canEdit:role==="SUPER_ADMIN"||f.uploadedBy===userId,url:await signRead(f.storagePath)})));return NextResponse.json({files,maxSize:MAX_SIZE,view:archived?"archived":"active"},{headers:{"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}

export async function POST(req:NextRequest){const session=await auth();if(!session?.user)return NextResponse.json({error:"Unauthorized"},{status:401});if(!base()||!process.env.SUPABASE_SERVICE_KEY)return NextResponse.json({error:"Storage is not configured."},{status:503});const body=await req.json().catch(()=>null);if(!body)return NextResponse.json({error:"Invalid request."},{status:400});const userId=String((session.user as any).id);if(body.op==="sign"){const name=clean(body.name),size=Number(body.size||0);if(!name||!Number.isFinite(size)||size<=0)return NextResponse.json({error:"Choose a valid file."},{status:400});if(size>MAX_SIZE)return NextResponse.json({error:"Maximum file size is 500 MB."},{status:413});try{await ensureBucket()}catch{return NextResponse.json({error:"Storage bucket is unavailable."},{status:503})}const path=`${WORKSPACE}/${new Date().getFullYear()}/${userId}/${crypto.randomUUID()}-${safeName(name)}`;const signed=await fetch(`${base()}/storage/v1/object/upload/sign/${BUCKET}/${path}`,{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:"{}"});const data=await signed.json().catch(()=>({}));if(!signed.ok)return NextResponse.json({error:data.message||data.error||"Could not prepare the upload."},{status:502});const relative=data.url||data.signedURL||data.signedUrl;if(!relative)return NextResponse.json({error:"Storage did not return an upload URL."},{status:502});return NextResponse.json({uploadUrl:relative.startsWith("http")?relative:`${base()}/storage/v1${relative}`,token:data.token||null,path,maxSize:MAX_SIZE})}if(body.op==="complete"){const path=clean(body.path,700),size=Number(body.size||0);if(!path.startsWith(`${WORKSPACE}/`)||!path.includes(`/${userId}/`)||path.includes(".."))return NextResponse.json({error:"Invalid file path."},{status:403});if(size<=0||size>MAX_SIZE)return NextResponse.json({error:"Invalid file size."},{status:400});const role=String((session.user as any).role),category=clean(body.category,40)||"GENERAL",clientId=body.clientId?clean(body.clientId,100):null,taskId=body.taskId?clean(body.taskId,100):null;if(!(await validateLinks(role,userId,clientId,taskId,category)))return NextResponse.json({error:"You cannot attach this file to the selected client or task."},{status:403});const info=await fetch(`${base()}/storage/v1/object/info/${BUCKET}/${path}`,{headers:headers()});if(!info.ok)return NextResponse.json({error:"Upload reached storage but could not be verified."},{status:409});const existing=await db.select({id:fileDocuments.id}).from(fileDocuments).where(and(eq(fileDocuments.workspaceId,WORKSPACE),eq(fileDocuments.storagePath,path))).limit(1);if(existing[0])return NextResponse.json({success:true,fileId:existing[0].id});const [row]=await db.insert(fileDocuments).values({workspaceId:WORKSPACE,uploadedBy:userId,name:clean(body.name)||"File",storagePath:path,mimeType:clean(body.mimeType,120)||"application/octet-stream",sizeBytes:size,category,clientId,taskId}).returning();await db.insert(auditLogs).values({workspaceId:WORKSPACE,userId,action:"file_uploaded",entity:"file_documents",entityId:row.id,newValues:JSON.stringify({name:row.name,size,category,clientId,taskId})} as any);return NextResponse.json({success:true,file:{...row,canEdit:true,isArchived:false}})}return NextResponse.json({error:"Unsupported operation."},{status:400})}

export async function PATCH(req:NextRequest){const session=await auth();if(!session?.user)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await req.json().catch(()=>null);if(!body)return NextResponse.json({error:"Invalid request."},{status:400});const id=clean(body.id,100),userId=String((session.user as any).id),role=String((session.user as any).role),op=clean(body.op,30)||"edit";if(!id)return NextResponse.json({error:"File id is required."},{status:400});const {row,allowed}=await editableFile(id,role,userId);if(!row)return NextResponse.json({error:"File not found."},{status:404});if(!allowed)return NextResponse.json({error:"You can only change files you uploaded."},{status:403});if(op==="archive"){await db.execute(sql`update file_documents set archived_at=now(),archived_by=${userId} where id=${id} and workspace_id=${WORKSPACE}`);await db.insert(auditLogs).values({workspaceId:WORKSPACE,userId,action:"file_archived",entity:"file_documents",entityId:id,newValues:JSON.stringify({name:row.name})} as any);return NextResponse.json({success:true,state:"archived"})}if(op==="restore"){await db.execute(sql`update file_documents set archived_at=null,archived_by=null where id=${id} and workspace_id=${WORKSPACE}`);await db.insert(auditLogs).values({workspaceId:WORKSPACE,userId,action:"file_restored",entity:"file_documents",entityId:id,newValues:JSON.stringify({name:row.name})} as any);return NextResponse.json({success:true,state:"active"})}const name=clean(body.name)||row.name,category=clean(body.category,40)||row.category,clientId=body.clientId?clean(body.clientId,100):null,taskId=body.taskId?clean(body.taskId,100):null;if(!(await validateLinks(role,userId,clientId,taskId,category)))return NextResponse.json({error:"You cannot link this file to the selected client or task."},{status:403});const [updated]=await db.update(fileDocuments).set({name,category,clientId,taskId}).where(and(eq(fileDocuments.id,id),eq(fileDocuments.workspaceId,WORKSPACE))).returning();await db.insert(auditLogs).values({workspaceId:WORKSPACE,userId,action:"file_updated",entity:"file_documents",entityId:id,newValues:JSON.stringify({name,category,clientId,taskId})} as any);return NextResponse.json({success:true,file:{...updated,canEdit:true}})}

export async function DELETE(req:NextRequest){const session=await auth();if(!session?.user)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await req.json().catch(()=>null);if(!body)return NextResponse.json({error:"Invalid request."},{status:400});const id=clean(body.id,100),userId=String((session.user as any).id),role=String((session.user as any).role);if(!id)return NextResponse.json({error:"File id is required."},{status:400});const {row,allowed}=await editableFile(id,role,userId);if(!row)return NextResponse.json({error:"File not found."},{status:404});if(!allowed)return NextResponse.json({error:"You can only delete files you uploaded."},{status:403});if(base()&&process.env.SUPABASE_SERVICE_KEY){const removal=await fetch(`${base()}/storage/v1/object/${BUCKET}/${row.storagePath}`,{method:"DELETE",headers:headers()});if(!removal.ok&&removal.status!==404){const d=await removal.json().catch(()=>({}));return NextResponse.json({error:d.message||"Could not delete the stored file."},{status:502})}}await db.delete(fileDocuments).where(and(eq(fileDocuments.id,id),eq(fileDocuments.workspaceId,WORKSPACE)));await db.insert(auditLogs).values({workspaceId:WORKSPACE,userId,action:"file_deleted",entity:"file_documents",entityId:id,oldValues:JSON.stringify({name:row.name,storagePath:row.storagePath,size:row.sizeBytes})} as any);return NextResponse.json({success:true,state:"deleted"})}
