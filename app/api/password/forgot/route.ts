export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {createHash,randomBytes} from "crypto";
import {db,users,passwordResetTokens,auditLogs} from "@/lib/db";
import {eq,and,gte,isNull} from "drizzle-orm";
import {escapeHtml,safeHttpUrl} from "@/lib/approval-security";
export async function POST(req:NextRequest){const body=await req.json().catch(()=>null),email=String(body?.email??"").trim().toLowerCase(),generic={success:true,message:"If this email exists, a reset link has been sent."},response=()=>NextResponse.json(generic,{headers:{"Cache-Control":"no-store","X-Content-Type-Options":"nosniff"}});if(!email||email.length>254||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return response();const [user]=await db.select({id:users.id,email:users.email,name:users.name,isActive:users.isActive,workspaceId:users.workspaceId}).from(users).where(eq(users.email,email)).limit(1);if(!user?.isActive)return response();const cooldown=new Date(Date.now()-2*60*1000),[recent]=await db.select({id:passwordResetTokens.id}).from(passwordResetTokens).where(and(eq(passwordResetTokens.userId,user.id),gte(passwordResetTokens.createdAt,cooldown),isNull(passwordResetTokens.usedAt))).limit(1);if(recent)return response();const token=randomBytes(32).toString("hex"),tokenHash=createHash("sha256").update(token).digest("hex"),expiresAt=new Date(Date.now()+30*60*1000);await db.insert(passwordResetTokens).values({userId:user.id,tokenHash,expiresAt});const candidate=process.env.NEXTAUTH_URL||req.nextUrl.origin,base=safeHttpUrl(candidate);if(base&&process.env.RESEND_API_KEY){const resetUrl=`${base.replace(/\/$/,"")}/reset-password?token=${token}`,html=`<h2>Reset your password</h2><p>Hello ${escapeHtml(user.name)},</p><p>This link expires in 30 minutes.</p><p><a href="${escapeHtml(resetUrl)}">Reset password</a></p>`;await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.EMAIL_FROM||"VIVIT ERP <onboarding@resend.dev>",to:[user.email],subject:"Reset your VIVIT ERP password",html})}).catch(()=>null)}else if(process.env.NODE_ENV!=="production"&&base)console.info("Password reset requested for development user",user.id);await db.insert(auditLogs).values({workspaceId:user.workspaceId,userId:user.id,action:"password_reset_requested",entity:"users",entityId:user.id,newValues:"{}"});return response()}
