export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {db,users,emailVerificationCodes} from "@/lib/db";
import {eq,gt,and} from "drizzle-orm";
import bcrypt from "bcryptjs";
import crypto from "crypto";
export async function GET(){return NextResponse.json({configured:Boolean(process.env.RESEND_API_KEY)},{headers:{"Cache-Control":"no-store"}})}
export async function POST(req:NextRequest){try{const body=await req.json().catch(()=>null),normalizedEmail=String(body?.email||"").trim().toLowerCase();if(!/^[a-z0-9._%+-]+@gmail\.com$/i.test(normalizedEmail))return NextResponse.json({error:"Please use a valid Gmail address"},{status:400});if((await db.select({id:users.id}).from(users).where(eq(users.email,normalizedEmail))).length)return NextResponse.json({error:"Email already registered"},{status:409});if(!process.env.RESEND_API_KEY)return NextResponse.json({error:"Email verification is being configured. Please contact the Super Admin."},{status:503});const cooldown=new Date(Date.now()-60_000),[recent]=await db.select({id:emailVerificationCodes.id,createdAt:emailVerificationCodes.createdAt}).from(emailVerificationCodes).where(and(eq(emailVerificationCodes.email,normalizedEmail),gt(emailVerificationCodes.createdAt,cooldown))).limit(1);if(recent)return NextResponse.json({error:"Please wait 60 seconds before requesting another code"},{status:429,headers:{"Retry-After":"60"}});const code=String(crypto.randomInt(100000,1000000)),codeHash=await bcrypt.hash(code,10),expiresAt=new Date(Date.now()+10*60*1000);await db.insert(emailVerificationCodes).values({email:normalizedEmail,codeHash,expiresAt,attempts:0}).onConflictDoUpdate({target:emailVerificationCodes.email,set:{codeHash,expiresAt,attempts:0,createdAt:new Date()}});const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.OTP_FROM_EMAIL||"VIVIT ERP <access@vivitgroup.com>",to:[normalizedEmail],subject:"Your VIVIT ERP verification code",html:`<div style="font-family:Arial;padding:28px;color:#201F20"><h2>VIVIT ERP</h2><p>Your verification code is:</p><div style="font-size:34px;font-weight:800;letter-spacing:8px">${code}</div><p>This code expires in 10 minutes. Never share it.</p></div>`}),signal:AbortSignal.timeout(8000)});if(!response.ok){await db.delete(emailVerificationCodes).where(eq(emailVerificationCodes.email,normalizedEmail));return NextResponse.json({error:"Could not send the verification email"},{status:502})}return NextResponse.json({success:true},{headers:{"Cache-Control":"no-store"}})}catch(error){console.error("OTP error",error);return NextResponse.json({error:"Could not send verification code"},{status:500})}}
