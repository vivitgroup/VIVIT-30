export const dynamic="force-dynamic";
import {NextRequest,NextResponse} from "next/server";
import {db,mediaMetrics,apiKeys,clients} from "@/lib/db";
import {eq,and,gte,sum,inArray} from "drizzle-orm";
import crypto from "crypto";
const READ_PERMISSIONS=new Set(["read","read_write","admin"]);
async function authenticateAPIKey(req:NextRequest){const raw=req.headers.get("x-api-key")??req.headers.get("authorization")?.replace(/^Bearer\s+/i,"");if(!raw)return null;const keyHash=crypto.createHash("sha256").update(raw).digest("hex");const [key]=await db.select().from(apiKeys).where(and(eq(apiKeys.keyHash,keyHash),eq(apiKeys.isActive,true))).limit(1);if(!key||!READ_PERMISSIONS.has(String(key.permissions||"")))return null;await db.update(apiKeys).set({lastUsedAt:new Date()}).where(and(eq(apiKeys.id,key.id),eq(apiKeys.workspaceId,key.workspaceId)));return key}
export async function GET(req:NextRequest){const key=await authenticateAPIKey(req);if(!key)return NextResponse.json({error:"Invalid API key"},{status:401});const active=await db.select({id:clients.id}).from(clients).where(and(eq(clients.workspaceId,key.workspaceId),eq(clients.isActive,true))),ids=active.map(x=>x.id),monthStart=new Date(new Date().getFullYear(),new Date().getMonth(),1);const metrics=ids.length?await db.select({platform:mediaMetrics.platform,adSpend:sum(mediaMetrics.adSpend),leads:sum(mediaMetrics.leads),revenue:sum(mediaMetrics.revenue),roas:sum(mediaMetrics.roas)}).from(mediaMetrics).where(and(eq(mediaMetrics.workspaceId,key.workspaceId),inArray(mediaMetrics.clientId,ids),gte(mediaMetrics.date,monthStart))).groupBy(mediaMetrics.platform):[];return NextResponse.json({data:metrics,period:"MTD"},{headers:{"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}
