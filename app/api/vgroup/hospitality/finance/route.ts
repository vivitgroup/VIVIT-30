import {NextResponse} from "next/server";
import {getVGroupSql} from "@/lib/vgroup/db";
import {apiErrorResponse,requireApiPermission} from "@/lib/vgroup/api-access";
import {generateOwnerStatement} from "@/lib/vgroup/operations";
export const dynamic="force-dynamic";
const uuid=/^[0-9a-f-]{36}$/i;
type LiveFinanceRow={id:string;source:string;guest_name:string;check_in:string;check_out:string;currency:string;gross_amount:number|string;platform_fee:number|string;company_commission:number|string;net_owner_amount:number|string;status:string};
export async function GET(request:Request){try{await requireApiPermission("hospitality","finance:view");const sql=getVGroupSql();const url=new URL(request.url);const propertyId=url.searchParams.get("propertyId")||"";if(propertyId&&!uuid.test(propertyId))return NextResponse.json({error:"invalid_property_id"},{status:400,headers:{"Cache-Control":"private, no-store"}});if(propertyId){const [property]=await sql`select id::text,name,owner_id::text from hospitality.properties where id=${propertyId}::uuid and archived_at is null limit 1`;if(!property)return NextResponse.json({error:"property_not_found"},{status:404,headers:{"Cache-Control":"private, no-store"}});const [refunds,deposits,reservations,airbnbFinance]=await Promise.all([sql`select r.id::text,r.reservation_id::text,r.currency,r.amount,r.reason,r.status,r.created_at from hospitality.refunds r join hospitality.reservations x on x.id=r.reservation_id where x.property_id=${propertyId}::uuid order by r.created_at desc limit 100`,sql`select d.id::text,d.reservation_id::text,d.currency,d.amount,d.held_amount,d.released_amount,d.status,d.created_at from hospitality.security_deposits d join hospitality.reservations x on x.id=d.reservation_id where x.property_id=${propertyId}::uuid order by d.created_at desc limit 100`,sql<LiveFinanceRow[]>`select id::text,source,guest_name,check_in::text,check_out::text,currency,gross_amount,platform_fee,company_commission,net_owner_amount,status from hospitality.reservations where property_id=${propertyId}::uuid and archived_at is null and status not in ('cancelled','no_show') order by check_in desc limit 250`,sql<LiveFinanceRow[]>`select id::text,'airbnb'::text source,coalesce(nullif(summary,''),'Airbnb stay / block') guest_name,starts_on::text check_in,ends_on::text check_out,currency,coalesce(gross_amount,0) gross_amount,coalesce(airbnb_fee,0) platform_fee,0::numeric company_commission,coalesce(net_payout,0) net_owner_amount,finance_status status from hospitality.calendar_blocks where property_id=${propertyId}::uuid and archived_at is null and finance_status in ('captured','verified','reconciled') order by starts_on desc limit 250`]);const liveFinance=[...Array.from(reservations),...Array.from(airbnbFinance)].sort((a,b)=>b.check_in.localeCompare(a.check_in));return NextResponse.json({property:{id:property.id,name:property.name},statements:[],payouts:[],refunds:Array.from(refunds),deposits:Array.from(deposits),reservations:liveFinance,propertyScoped:true},{headers:{"Cache-Control":"private, no-store"}})}const [statements,payouts,refunds,deposits]=await Promise.all([sql`select s.id::text,s.owner_id::text,o.full_name owner_name,s.period_start,s.period_end,s.currency,s.gross_revenue,s.total_expenses,s.total_fees,s.net_payable,s.status from hospitality.owner_statements s join hospitality.owners o on o.id=s.owner_id order by s.period_end desc limit 100`,sql`select p.id::text,p.owner_id::text,o.full_name owner_name,p.statement_id::text,p.currency,p.amount,p.status,p.due_at,p.paid_at from hospitality.owner_payouts p join hospitality.owners o on o.id=p.owner_id order by p.created_at desc limit 100`,sql`select r.id::text,r.reservation_id::text,r.currency,r.amount,r.reason,r.status,r.created_at from hospitality.refunds r order by r.created_at desc limit 100`,sql`select d.id::text,d.reservation_id::text,d.currency,d.amount,d.held_amount,d.released_amount,d.status,d.created_at from hospitality.security_deposits d order by d.created_at desc limit 100`]);return NextResponse.json({statements:Array.from(statements),payouts:Array.from(payouts),refunds:Array.from(refunds),deposits:Array.from(deposits),reservations:[],propertyScoped:false},{headers:{"Cache-Control":"private, no-store"}})}catch(e){return apiErrorResponse(e)}}
export async function POST(request:Request){try{await requireApiPermission("hospitality","finance:create");const body=await request.json() as {ownerId?:string;periodStart?:string;periodEnd?:string};if(!body.ownerId||!/^\d{4}-\d{2}-\d{2}$/.test(body.periodStart||"")||!/^\d{4}-\d{2}-\d{2}$/.test(body.periodEnd||""))return NextResponse.json({error:"invalid_statement_payload"},{status:400});return NextResponse.json({statement:await generateOwnerStatement(body.ownerId,body.periodStart!,body.periodEnd!)},{status:201})}catch(e){return apiErrorResponse(e)}}
