import {NextResponse} from "next/server";
import {getVGroupSql} from "@/lib/vgroup/db";
import {apiErrorResponse,requireApiPermission} from "@/lib/vgroup/api-access";
export const dynamic="force-dynamic";

export async function GET(){try{await requireApiPermission("hospitality","owners:view");const sql=getVGroupSql();const rows=await sql`select o.id::text,o.full_name,o.email,o.phone,o.status,count(p.id)::int properties from hospitality.owners o left join hospitality.properties p on p.owner_id=o.id and p.archived_at is null where o.archived_at is null group by o.id order by o.created_at desc limit 250`;return NextResponse.json({owners:Array.from(rows)},{headers:{"Cache-Control":"private, no-store"}})}catch(error){return apiErrorResponse(error)}}

export async function POST(request:Request){try{const session=await requireApiPermission("hospitality","owners:create");const body=await request.json() as Record<string,unknown>;const fullName=String(body.fullName??"").trim();const email=body.email?String(body.email).trim().toLowerCase():null;const phone=body.phone?String(body.phone).trim():null;if(fullName.length<2)return NextResponse.json({error:"Owner name is required"},{status:400});const sql=getVGroupSql();const [row]=await sql`insert into hospitality.owners(business_unit_id,full_name,email,phone,status) select id,${fullName},${email},${phone},'active' from vgroup.business_units where code='hospitality' returning id::text,full_name,email,phone,status`;await sql`insert into vgroup.audit_logs(business_unit_id,user_id,action,entity_type,entity_id,new_value) select id,${session.userId}::uuid,'owner.create','owner',${row.id}::uuid,jsonb_build_object('full_name',${fullName},'email',${email}) from vgroup.business_units where code='hospitality'`;return NextResponse.json({owner:row},{status:201})}catch(error){return apiErrorResponse(error)}}
