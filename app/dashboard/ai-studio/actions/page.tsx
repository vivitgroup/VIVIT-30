"use client";
import {useEffect,useState} from "react";
export default function ActionCenterPage(){const[data,setData]=useState<any>({actions:[]}),[busy,setBusy]=useState(""),[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/copilot/action-center",{cache:"no-store"}),d=await r.json().catch(()=>({actions:[]}));setData(d)}useEffect(()=>{load();const t=setInterval(load,30000);return()=>clearInterval(t)},[]);async function run(a:any){if(!a.execution)return;setBusy(a.id);setMsg("");try{const r=await fetch("/api/copilot/action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(a.execution)}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"Action failed");if(d.href?.startsWith("https://wa.me/"))window.open(d.href,"_blank","noopener,noreferrer");setMsg(d.message||"Action completed");await fetch("/api/copilot/outcome",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"EXECUTED",title:a.title,detail:a.reason,clientId:a.clientId,recommendationId:a.id})}).catch(()=>null);await load()}catch(e:any){setMsg(e.message)}finally{setBusy("")}}return <div className="ac-shell"><style>{CSS}</style><header><div><span>VIVIT COPILOT</span><h1>AI Action Center</h1><p>Recommendations are prepared from live ERP data. Nothing sensitive is executed without your click.</p></div><button className="btn btn-secondary" onClick={load}>Refresh</button></header>{msg&&<div className="ac-msg">{msg}</div>}<div className="ac-grid">{(data.actions||[]).map((a:any)=><article key={a.id} className={`ac-card ${String(a.priority).toLowerCase()}`}><div className="ac-top"><span>{a.kind.replaceAll("_"," ")}</span><b>{a.priority}</b></div><h3>{a.title}</h3><p>{a.reason}</p><div className="ac-actions">{a.href&&<a href={a.href} className="btn btn-ghost btn-sm">Open source</a>}{a.execution&&<button className="btn btn-primary btn-sm" disabled={busy===a.id} onClick={()=>run(a)}>{busy===a.id?"Working…":a.execution.action==="prepare_whatsapp"?"Prepare WhatsApp":a.execution.action==="create_task"?"Create Task":"Add Follow-up"}</button>}</div></article>)}{!data.actions?.length&&<div className="ac-empty">No recommended actions right now.</div>}</div></div>}
const CSS=`.ac-shell{display:grid;gap:16px;max-width:1400px;margin:0 auto}.ac-shell>header{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;padding:20px;border:1px solid var(--card-border);background:var(--card-bg);border-radius:20px}.ac-shell header span{font-size:10px;font-weight:900;letter-spacing:.14em;color:var(--text-muted)}.ac-shell h1{margin:4px 0;font-size:28px}.ac-shell header p{margin:0;color:var(--text-muted);font-size:12px}.ac-msg{padding:12px 14px;border:1px solid var(--card-border);border-radius:12px;background:var(--bg-subtle);font-size:12px}.ac-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.ac-card{padding:16px;border:1px solid var(--card-border);border-radius:18px;background:var(--card-bg);display:grid;gap:9px}.ac-card.high{border-top:3px solid #dc2626}.ac-card.medium{border-top:3px solid #d97706}.ac-top{display:flex;justify-content:space-between;gap:10px;font-size:9px;font-weight:900;letter-spacing:.08em;color:var(--text-muted)}.ac-card h3{font-size:15px;margin:0}.ac-card p{font-size:11px;color:var(--text-muted);margin:0;line-height:1.6}.ac-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:4px}.ac-empty{grid-column:1/-1;padding:40px;text-align:center;border:1px dashed var(--card-border);border-radius:18px;color:var(--text-muted)}@media(max-width:900px){.ac-grid{grid-template-columns:1fr 1fr}}@media(max-width:600px){.ac-grid{grid-template-columns:1fr}.ac-shell>header{flex-direction:column}}`;
