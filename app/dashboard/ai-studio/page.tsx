export const dynamic="force-dynamic";
import {auth} from "@/lib/auth";
import {redirect} from "next/navigation";
import Link from "next/link";
import {VivitoStudioConsole} from "@/components/ai/VivitoStudioConsole";
export default async function AIStudioPage(){const session=await auth();if(!session?.user)redirect("/login");const role=String(session.user.role||"CLIENT");return <div style={{display:"flex",flexDirection:"column",gap:18,maxWidth:1000}}><div><h1 className="page-title">✦ VIVITO</h1><p className="page-subtitle">VIVIT Operating Intelligence · live ERP reasoning for {role.replace(/_/g," ")}</p></div><VivitoStudioConsole role={role}/><div className="card" style={{borderTop:"3px solid #C98A3D"}}><div className="card-body"><h2 className="card-title">VIVITO Control</h2><p className="page-subtitle" style={{marginTop:6}}>Ask questions, propose actions, confirm governed writes and see the result in the same surface. The floating VIVITO button remains available on every dashboard page.</p><div style={{display:"flex",gap:10,flexWrap:"wrap",marginTop:14}}><Link className="btn btn-secondary" href="/dashboard/ai-studio/operations">Operator Control</Link></div></div></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:12}}>{[{t:"Operating Context",d:"Tasks, deadlines, blockers, approvals and role-safe workspace facts",i:"⏱️"},{t:"Decision Intelligence",d:"Root-cause diagnosis, evidence checks, confidence and prioritized actions",i:"🧠"},{t:"Marketing & Creative",d:"Strategy, funnels, offers, content, design and creative performance",i:"📣"},{t:"Media & Measurement",d:"Meta performance, KPIs, tracking, attribution, optimization and scaling",i:"📊"}].map(x=><section className="card" key={x.t} aria-label={x.t}><div className="card-body"><span style={{fontSize:28}} aria-hidden="true">{x.i}</span><h3 style={{marginTop:8}}>{x.t}</h3><p className="page-subtitle" style={{marginTop:4}}>{x.d}</p></div></section>)}</div></div>}
