export const dynamic="force-dynamic";
import { auth } from "@/lib/auth";
import { redirect } from "next/navigation";
export default async function AIStudioPage(){const session=await auth();if(!session?.user)redirect("/login");const role=String((session.user as any).role||"CLIENT");const qs=["How does the creative approval flow work?","How do I add a post to the calendar?","Explain ROAS, CPL and CPA","How should I structure a Meta campaign?","How do I read the client portal?","Where do Content Plans and Strategies go?","What can my role access?","How do payment reminders work?"];return <div style={{display:"flex",flexDirection:"column",gap:18,maxWidth:1000}}><div><h1 className="page-title">✨ VIVIT Assistant</h1><p className="page-subtitle">System guide + marketing and media-buying help for {role.replace(/_/g," ")}</p></div><div className="card" style={{borderTop:"3px solid #7C3AED"}}><div className="card-body"><h2 className="card-title">The assistant is available everywhere</h2><p className="page-subtitle" style={{marginTop:6}}>Use the floating ✨ button at the bottom-right on any page. It explains every workflow according to your permissions and answers marketing/media-buying questions.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(230px,1fr))",gap:10,marginTop:16}}>{qs.map(q=><div key={q} style={{padding:12,borderRadius:12,background:"var(--bg-tertiary)",border:"1px solid var(--card-border)",fontSize:12.5,color:"var(--text-secondary)"}}>💬 {q}</div>)}</div></div></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:12}}>{[{t:"System Help",d:"Pages, roles, approvals, calendar, files and settings",i:"🧭"},{t:"Marketing",d:"Strategy, funnels, positioning, content and campaign planning",i:"📣"},{t:"Media Buying",d:"Meta/TikTok structure, KPIs, optimization and scaling",i:"📊"},{t:"Client Workflows",d:"Approvals, portal, content plan, calendar and payments",i:"🤝"}].map(x=><div className="card" key={x.t}><div className="card-body"><span style={{fontSize:28}}>{x.i}</span><h3 style={{marginTop:8}}>{x.t}</h3><p className="page-subtitle" style={{marginTop:4}}>{x.d}</p></div></div>)}</div></div>}
