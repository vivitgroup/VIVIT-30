export const dynamic="force-dynamic";
import {auth} from "@/lib/auth";
import {redirect} from "next/navigation";
import Link from "next/link";
export default async function AIStudioPage(){const session=await auth();if(!session?.user)redirect("/login");const role=String((session.user as any).role||"CLIENT");const qs=["What tasks do we have today?","What are the closest deadlines?","Which tasks are overdue?","How does the creative approval flow work?","How do I add a post to the calendar?","Explain ROAS, CPL and CPA","How should I structure a Meta campaign?","What can my role access?"];return <div style={{display:"flex",flexDirection:"column",gap:18,maxWidth:1000}}><div><h1 className="page-title">✦ VIVITO</h1><p className="page-subtitle">VIVIT Operating Intelligence · live ERP reasoning for {role.replace(/_/g," ")}</p></div><div className="card" style={{borderTop:"3px solid #C98A3D"}}><div className="card-body"><h2 className="card-title">Ask VIVITO about your real workspace</h2><p className="page-subtitle" style={{marginTop:6}}>Use the floating ✦ button on any page. VIVITO reasons from the ERP data your role is allowed to access, then combines the right marketing, creative, sales, analytics and operations expertise for the decision.</p><div style={{display:"flex",gap:10,flexWrap:"wrap",marginTop:14}}><Link className="btn btn-secondary" href="/dashboard/ai-studio/operations">Operator Control</Link></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(230px,1fr))",gap:10,marginTop:16}}>{qs.map(q=><div key={q} style={{padding:12,borderRadius:12,background:"var(--bg-tertiary)",border:"1px solid var(--card-border)",fontSize:12.5,color:"var(--text-secondary)"}}>💬 {q}</div>)}</div></div></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:12}}>{[{t:"Operating Context",d:"Tasks, deadlines, blockers, approvals and role-safe workspace facts",i:"⏱️"},{t:"Decision Intelligence",d:"Root-cause diagnosis, evidence checks, confidence and prioritized actions",i:"🧠"},{t:"Marketing & Creative",d:"Strategy, funnels, offers, content, design and creative performance",i:"📣"},{t:"Media & Measurement",d:"Meta performance, KPIs, tracking, attribution, optimization and scaling",i:"📊"}].map(x=><div className="card" key={x.t}><div className="card-body"><span style={{fontSize:28}}>{x.i}</span><h3 style={{marginTop:8}}>{x.t}</h3><p className="page-subtitle" style={{marginTop:4}}>{x.d}</p></div></div>)}</div></div>}
