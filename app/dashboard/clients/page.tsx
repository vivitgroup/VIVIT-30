export const dynamic = "force-dynamic";
import { auth } from "@/lib/auth";
import { redirect } from "next/navigation";
import { db, clients, users, auditLogs, sql } from "@/lib/db";
import { eq, and } from "drizzle-orm";
import { Role } from "@/lib/types";
import Link from "next/link";
type SessionUser={role?:Role|string;id?:string;workspaceId?:string};

const money=(n:number)=>new Intl.NumberFormat("en-EG",{style:"currency",currency:"EGP",maximumFractionDigits:0}).format(Number(n||0));
async function archiveClient(fd:FormData){"use server";const session=await auth();if(!session?.user)throw new Error("Unauthorized");const sessionUser=session.user as unknown as SessionUser,role=sessionUser.role as Role,userId=String(sessionUser.id||""),workspaceId=String(sessionUser.workspaceId||""),id=String(fd.get("id")||"");if(!workspaceId)throw new Error("Workspace unavailable");const [client]=await db.select({id:clients.id,companyName:clients.companyName,accountManagerId:clients.accountManagerId}).from(clients).where(and(eq(clients.id,id),eq(clients.workspaceId,workspaceId),eq(clients.isActive,true))).limit(1);if(!client)throw new Error("Client not found");if(role!==Role.SUPER_ADMIN&&!(role===Role.ACCOUNT_MANAGER&&client.accountManagerId===userId))throw new Error("You can only archive clients assigned to you.");const changed=Array.from(await db.execute(sql`update clients set is_active=false,archived_at=now(),archived_by=${userId},updated_at=now() where id=${id} and workspace_id=${workspaceId} and is_active=true returning id`));if(!changed.length)throw new Error("Client was already archived or is unavailable");await db.insert(auditLogs).values({workspaceId,userId,action:"client_archived",entity:"clients",entityId:id,newValues:JSON.stringify({companyName:client.companyName})});const {revalidatePath}=await import("next/cache");revalidatePath("/dashboard/clients");revalidatePath("/dashboard/archive");}

export default async function ClientsPage({searchParams}:{searchParams?:Promise<Record<string,string|undefined>>}) {
  const session = await auth();if (!session?.user) redirect("/login");
  const sessionUser=session.user as unknown as SessionUser,role=sessionUser.role as Role,userId=String(sessionUser.id||""),workspaceId=String(sessionUser.workspaceId||"");if(!workspaceId)redirect("/login?reason=workspace_missing");
  if (![Role.SUPER_ADMIN,Role.ACCOUNT_MANAGER,Role.MEDIA_BUYER,Role.ACCOUNTANT].includes(role)) redirect("/dashboard");
  const canAddClient=[Role.SUPER_ADMIN,Role.ACCOUNT_MANAGER,Role.ACCOUNTANT].includes(role),canArchiveClient=[Role.SUPER_ADMIN,Role.ACCOUNT_MANAGER].includes(role),params=(await searchParams)||{},q=String(params.q||"").trim().toLowerCase();
  const portfolioScope=and(eq(clients.workspaceId,workspaceId),eq(clients.isActive,true),role===Role.ACCOUNT_MANAGER?eq(clients.accountManagerId,userId):role===Role.MEDIA_BUYER?eq(clients.mediaBuyerId,userId):undefined);
  const [allClientsRaw, allAMs] = await Promise.all([
    db.select({id:clients.id,companyName:clients.companyName,industry:clients.industry,healthScore:clients.healthScore,churnRisk:clients.churnRisk,monthlyRetainer:clients.monthlyRetainer,lifetimeValue:clients.lifetimeValue,isActive:clients.isActive,accountManagerId:clients.accountManagerId,contractEnd:clients.contractEnd,mediaBudget:clients.mediaBudget,createdAt:clients.createdAt}).from(clients).where(portfolioScope).orderBy(clients.healthScore),
    db.select({id:users.id,name:users.name}).from(users).where(and(eq(users.workspaceId,workspaceId),eq(users.role,"ACCOUNT_MANAGER"),eq(users.isActive,true))),
  ]);
  const amMap=Object.fromEntries(allAMs.map(u=>[u.id,u.name])),allClients=q?allClientsRaw.filter(c=>[c.companyName,c.industry,amMap[c.accountManagerId||""]].some(v=>String(v||"").toLowerCase().includes(q))):allClientsRaw,totalARR=allClientsRaw.reduce((s,c)=>s+Number(c.monthlyRetainer||0)*12,0),atRisk=allClientsRaw.filter(c=>c.churnRisk==="HIGH").length,avgHealth=allClientsRaw.length?Math.round(allClientsRaw.reduce((s,c)=>s+Number(c.healthScore||0),0)/allClientsRaw.length):0,RISK_BADGE:Record<string,string>={HIGH:"badge-red",MEDIUM:"badge-amber",LOW:"badge-green"};
  return <div className="clients-page"><style>{`.clients-page{display:flex;flex-direction:column;gap:18px;min-width:0}.clients-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;flex-wrap:wrap}.clients-actions{display:flex;gap:8px;flex-wrap:wrap}.clients-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.client-kpi{background:var(--card-bg);border:1px solid var(--card-border);border-radius:16px;padding:16px;box-shadow:var(--shadow-sm)}.client-kpi span{display:block;font-size:12px;color:var(--text-muted);font-weight:650}.client-kpi b{display:block;margin-top:7px;font-size:22px;color:var(--text-primary)}.client-toolbar{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap}.client-search{display:flex;gap:8px;flex:1;min-width:240px;max-width:520px}.client-search input{min-width:0}.clients-table-wrap{overflow:auto}.clients-mobile{display:none}.client-card{background:var(--card-bg);border:1px solid var(--card-border);border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:10px}.client-top{display:flex;align-items:center;justify-content:space-between;gap:10px}.client-ident{display:flex;align-items:center;gap:10px;min-width:0}.client-avatar{width:38px;height:38px;border-radius:11px;display:grid;place-items:center;color:white;font-weight:800;font-size:11px;flex:0 0 auto}.client-name{font-size:14px;font-weight:800;color:var(--text-primary);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.client-muted{font-size:11px;color:var(--text-muted);margin-top:2px}.client-row{display:flex;justify-content:space-between;gap:12px}.client-row span:first-child{font-size:11px;color:var(--text-muted)}.client-row span:last-child{font-size:12px;color:var(--text-primary);text-align:right}.health-line{display:flex;align-items:center;gap:8px}.health-track{height:7px;flex:1;background:var(--bg-tertiary);border-radius:999px;overflow:hidden}.health-track i{display:block;height:100%;border-radius:999px}.client-empty{text-align:center;padding:42px 20px;color:var(--text-muted)}@media(max-width:900px){.clients-kpis{grid-template-columns:repeat(2,minmax(0,1fr))}.clients-table-wrap{display:none}.clients-mobile{display:grid;gap:10px}.clients-head h1{font-size:1.55rem}.client-toolbar{align-items:stretch}.client-search{max-width:none;width:100%}}@media(max-width:560px){.clients-actions{width:100%}.clients-actions a{flex:1;text-align:center}.clients-kpis{grid-template-columns:1fr 1fr}.client-kpi{padding:13px}.client-kpi b{font-size:18px}.client-search{display:grid;grid-template-columns:1fr auto}.client-search .btn{padding-inline:14px}}`}</style>
  <div className="clients-head"><div><h1 className="page-title">Clients</h1><p className="page-subtitle">{allClientsRaw.length} active clients · portfolio health and revenue overview</p></div><div className="clients-actions">{canArchiveClient&&<Link href="/dashboard/archive" className="btn btn-secondary" style={{textDecoration:"none"}}>Archive</Link>}{[Role.SUPER_ADMIN,Role.ACCOUNTANT].includes(role)&&<Link href="/dashboard/clients/accounts-payment" className="btn btn-secondary" style={{textDecoration:"none"}}>Accounts Payment</Link>}{canAddClient&&<Link href="/dashboard/clients/new" className="btn btn-primary" style={{textDecoration:"none"}}>+ Add Client</Link>}</div></div>
  <div className="clients-kpis"><div className="client-kpi"><span>Total Clients</span><b>{allClientsRaw.length}</b></div><div className="client-kpi"><span>Annual Revenue</span><b>{money(totalARR)}</b></div><div className="client-kpi"><span>Avg Health</span><b>{avgHealth}%</b></div><div className="client-kpi"><span>At Risk</span><b>{atRisk}</b></div></div>
  <div className="client-toolbar"><form className="client-search" action="/dashboard/clients" method="GET"><input className="form-input" name="q" defaultValue={params.q||""} placeholder="Search client, industry or account manager"/><button className="btn btn-secondary" type="submit">Search</button></form>{q&&<Link href="/dashboard/clients" className="btn btn-ghost" style={{textDecoration:"none"}}>Clear</Link>}</div>
  <div className="card clients-table-wrap"><div className="card-body-flush"><table className="data-table"><thead><tr><th>Client</th><th>Industry</th><th>Account Manager</th><th>Health</th><th>Risk</th><th>Monthly</th><th>LTV</th><th>Actions</th></tr></thead><tbody>{allClients.map(c=>{const h=Math.round(Number(c.healthScore||0)),barColor=h>=80?"var(--green)":h>=60?"var(--amber)":"var(--red)";return <tr key={c.id}><td><div style={{display:"flex",alignItems:"center",gap:10}}><div className="avatar avatar-sm" style={{background:barColor,fontSize:11}}>{c.companyName.slice(0,2).toUpperCase()}</div><div><p style={{fontWeight:700,color:"var(--text-primary)",fontSize:13.5}}>{c.companyName}</p>{c.contractEnd&&<p style={{fontSize:11,color:"var(--text-muted)"}}>Contract ends {new Date(c.contractEnd).toLocaleDateString("en-GB",{month:"short",year:"numeric"})}</p>}</div></div></td><td>{c.industry||"—"}</td><td>{amMap[c.accountManagerId||""]||"—"}</td><td><div className="health-line" style={{minWidth:110}}><div className="health-track"><i style={{width:`${h}%`,background:barColor}}/></div><b style={{fontSize:11,color:barColor}}>{h}%</b></div></td><td><span className={`badge ${RISK_BADGE[c.churnRisk]||"badge-gray"}`}>{c.churnRisk}</span></td><td><b style={{color:"var(--text-primary)"}}>{money(Number(c.monthlyRetainer||0))}</b></td><td><b style={{color:"var(--vivit-blue)"}}>{money(Number(c.lifetimeValue||0))}</b></td><td><div style={{display:"flex",gap:6,flexWrap:"wrap"}}><Link href={`/dashboard/clients/${c.id}`} className="btn btn-ghost btn-sm" style={{textDecoration:"none"}}>Open →</Link>{canArchiveClient&&<form action={archiveClient}><input type="hidden" name="id" value={c.id}/><button className="btn btn-ghost btn-sm">Archive</button></form>}</div></td></tr>})}{!allClients.length&&<tr><td colSpan={8}><div className="client-empty"><div style={{fontSize:30,marginBottom:8}}>🏢</div><b>{q?"No matching clients":"No clients yet"}</b><p style={{fontSize:12,marginTop:4}}>{q?"Try another search term":"Create the first client to start the portfolio"}</p></div></td></tr>}</tbody></table></div></div>
  <div className="clients-mobile">{allClients.map(c=>{const h=Math.round(Number(c.healthScore||0)),barColor=h>=80?"var(--green)":h>=60?"var(--amber)":"var(--red)";return <div className="client-card" key={c.id}><div className="client-top"><div className="client-ident"><div className="client-avatar" style={{background:barColor}}>{c.companyName.slice(0,2).toUpperCase()}</div><div style={{minWidth:0}}><div className="client-name">{c.companyName}</div><div className="client-muted">{c.industry||"No industry"}</div></div></div><span className={`badge ${RISK_BADGE[c.churnRisk]||"badge-gray"}`}>{c.churnRisk}</span></div><div className="health-line"><div className="health-track"><i style={{width:`${h}%`,background:barColor}}/></div><b style={{fontSize:11,color:barColor}}>{h}%</b></div><div className="client-row"><span>Account Manager</span><span>{amMap[c.accountManagerId||""]||"—"}</span></div><div className="client-row"><span>Monthly</span><span>{money(Number(c.monthlyRetainer||0))}</span></div><div className="client-row"><span>LTV</span><span>{money(Number(c.lifetimeValue||0))}</span></div>{c.contractEnd&&<div className="client-row"><span>Contract End</span><span>{new Date(c.contractEnd).toLocaleDateString("en-GB")}</span></div>}<div style={{display:"grid",gridTemplateColumns:canArchiveClient?"1fr 1fr":"1fr",gap:8}}><Link href={`/dashboard/clients/${c.id}`} className="btn btn-secondary" style={{textDecoration:"none",textAlign:"center"}}>Open Client</Link>{canArchiveClient&&<form action={archiveClient}><input type="hidden" name="id" value={c.id}/><button className="btn btn-ghost" style={{width:"100%"}}>Archive</button></form>}</div></div>})}{!allClients.length&&<div className="client-card client-empty"><div style={{fontSize:30}}>🏢</div><b>{q?"No matching clients":"No clients yet"}</b></div>}</div>
  </div>;
}
