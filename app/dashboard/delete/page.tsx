"use client";
import {useCallback,useEffect,useMemo,useState} from "react";

type Row={id:string;entity:string;name:string;actor_name?:string|null;state_at:string;metadata?:{category?:string}};
type LifecycleResponse={error?:string;items?:Row[]};
const LABELS:Record<string,string>={client:"Clients",task:"Creative Tasks",campaign:"Campaigns",lead:"Sales Leads",file:"Files",media_plan:"Media Plans",calendar_event:"Calendar",leave_request:"Leave Requests",asset:"Assets",vendor:"Vendors",purchase_order:"Purchase Orders",time_entry:"Time Entries",expense_claim:"Expense Claims",project_budget:"Project Budgets",proposal:"Proposals",service_item:"Service Catalog",workflow_rule:"Workflow Rules",email_campaign:"Email Campaigns",workspace_role:"Workspace Roles",knowledge_article:"Knowledge Base",follow_up:"Sales Follow-ups",finance_record:"Finance Records",company_expense:"Company Expenses",contract:"Contracts",chart_account:"Chart of Accounts",journal_entry:"Journal Entries",contact:"Contacts"};
export default function DeleteCenterPage(){
 const [items,setItems]=useState<Row[]>([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState(""),[error,setError]=useState("");
 const load=useCallback(async()=>{setLoading(true);setError("");try{const r=await fetch("/api/lifecycle?view=deleted",{cache:"no-store"}),d=(await r.json().catch(()=>({}))) as LifecycleResponse;if(!r.ok)throw new Error(d.error||"Could not load Delete Center");setItems(d.items||[])}catch(e){setError(e instanceof Error?e.message:"Could not load Delete Center")}finally{setLoading(false)}},[]);
 useEffect(()=>{const timer=setTimeout(()=>void load(),0);return()=>clearTimeout(timer)},[load]);
 async function restore(row:Row){const key=`${row.entity}:${row.id}`;setBusy(key);setError("");try{const r=await fetch("/api/lifecycle",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({entity:row.entity,id:row.id,action:"restore_deleted"})}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"Could not restore record");await load()}catch(e){setError(e instanceof Error?e.message:"Could not restore record")}finally{setBusy("")}}
 const groups=useMemo(()=>{const map=new Map<string,Row[]>();for(const row of items){const key=row.metadata?.category||LABELS[row.entity]||row.entity.replaceAll("_"," ");map.set(key,[...(map.get(key)||[]),row])}return [...map.entries()].sort((a,b)=>a[0].localeCompare(b[0]))},[items]);
 const section=(title:string,rows:Row[])=><section className="card" key={title}><div className="card-header"><div><p className="card-title">{title}</p><p className="page-subtitle">{rows.length} deleted</p></div><span className="badge badge-gray">{rows.length}</span></div><div className="card-body-flush"><div className="responsive-table"><table className="data-table"><thead><tr><th>Name</th><th>Type</th><th>Deleted by</th><th>Deleted at</th><th>Action</th></tr></thead><tbody>{rows.map(row=>{const key=`${row.entity}:${row.id}`;return <tr key={key}><td><b>{row.name||"Untitled"}</b></td><td>{LABELS[row.entity]||row.entity.replaceAll("_"," ")}</td><td>{row.actor_name||"Unknown user"}</td><td>{row.state_at?new Date(row.state_at).toLocaleString():"—"}</td><td><button className="btn btn-secondary btn-sm" disabled={busy===key} onClick={()=>restore(row)}>Restore</button></td></tr>})}</tbody></table></div></div></section>;
 return <div style={{display:"grid",gap:18}}><div><h1 className="page-title">Delete Center</h1><p className="page-subtitle">Super Admin audit view for every lifecycle-enabled business record, including who deleted it and when.</p></div>{error&&<div className="form-error">{error}</div>}{loading?<div className="card"><div className="empty-state">Loading deleted records…</div></div>:groups.length?groups.map(([title,rows])=>section(title,rows)):<div className="card"><div className="empty-state">No deleted business records.</div></div>}</div>;
}