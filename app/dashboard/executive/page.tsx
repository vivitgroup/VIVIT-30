export const dynamic="force-dynamic";
import {auth} from "@/lib/auth";
import {redirect} from "next/navigation";
import {db,sql} from "@/lib/db";
import Link from "next/link";
type SessionUser={role?:string;workspaceId?:string};
type ClientRow={id:string;company_name:string};
type TaskRow={id:string;title:string;status:string;deadline:string|number|Date;revision_count:unknown;company_name:string};
type MediaRow={company_name:string;id:string;name:string;reported_result_label:string|null;archived_at:unknown;spend:unknown;results:unknown;atc:unknown;purchases:unknown;revenue:unknown};
type BillingRow={company_name:string;outstanding:unknown;status:string|null};
const rows=<T extends Record<string,unknown>>(value:unknown)=>Array.from(value as Iterable<T>);
const n=(v:unknown)=>Number(v||0),money=(v:unknown)=>`${Math.round(n(v)).toLocaleString("en-EG")} EGP`;
export default async function ExecutivePage(){const s=await auth();if(!s?.user)redirect("/login");const sessionUser=s.user as unknown as SessionUser,workspaceId=String(sessionUser.workspaceId||"");if(!workspaceId)redirect("/login");if(String(sessionUser.role)!=="SUPER_ADMIN")redirect("/dashboard");const [clock]=rows<{now_ms:number|string}>(await db.execute(sql`select (extract(epoch from now())*1000)::bigint now_ms`)),nowMs=n(clock?.now_ms),clients=rows<ClientRow>(await db.execute(sql`select id,company_name from clients where workspace_id=${workspaceId} and is_active=true order by company_name`)),ids=clients.map(x=>String(x.id));const tasks=rows<TaskRow>(await db.execute(sql`select t.id,t.title,t.status,t.deadline,t.revision_count,c.company_name from creative_tasks t join clients c on c.id=t.client_id where t.workspace_id=${workspaceId} and c.workspace_id=${workspaceId} and c.is_active=true and t.archived_at is null and t.deleted_at is null and t.status not in ('COMPLETED','REJECTED') order by t.deadline asc limit 200`));let media:MediaRow[]=[],billing:BillingRow[]=[];if(ids.length){media=rows<MediaRow>(await db.execute(sql`select c.company_name,a.id,a.name,a.reported_result_label,a.archived_at,coalesce(sum(p.spend),0) spend,coalesce(sum(p.results),0) results,coalesce(sum(p.add_to_cart),0) atc,coalesce(sum(p.purchases),0) purchases,coalesce(sum(p.revenue),0) revenue from ad_campaigns a join clients c on c.id=a.client_id left join ad_performance_daily p on p.campaign_id=a.id and p.date>=date_trunc('month',now()) and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null where a.client_id in (${sql.join(ids.map(id=>sql`${id}`),sql`,`)}) group by c.company_name,a.id,a.name,a.reported_result_label,a.archived_at order by spend desc`));billing=rows<BillingRow>(await db.execute(sql`select c.company_name,p.amount_remaining outstanding,p.payment_status status from client_payment_profiles p join clients c on c.id=p.client_id where c.workspace_id=${workspaceId} order by p.amount_remaining desc`));}const overdue=tasks.filter(t=>new Date(t.deadline).getTime()<nowMs),review=tasks.filter(t=>["REVIEW","REVISION","APPROVED"].includes(String(t.status))),spend=media.reduce((a,x)=>a+n(x.spend),0),revenue=media.reduce((a,x)=>a+n(x.revenue),0),outstanding=billing.reduce((a,x)=>a+n(x.outstanding),0),activeMedia=media.filter(x=>!x.archived_at),zero=activeMedia.filter(x=>n(x.spend)>0&&n(x.results)===0),highRev=tasks.filter(x=>n(x.revision_count)>=2),riskByClient=clients.map(c=>{const name=c.company_name,od=overdue.filter(x=>x.company_name===name).length,z=zero.filter(x=>x.company_name===name).length,rv=highRev.filter(x=>x.company_name===name).length,b=n(billing.find(x=>x.company_name===name)?.outstanding),score=Math.min(100,od*18+z*22+rv*12+(b>0?20:0));return{name,score,od,z,rv,b}}).sort((a,b)=>b.score-a.score),decisions=[...overdue.slice(0,4).map(x=>({title:"Recover overdue delivery",body:`${x.company_name} · ${x.title}`,href:`/dashboard/creative/${x.id}`,level:"critical"})),...zero.slice(0,4).map(x=>({title:"Diagnose campaign spend without result",body:`${x.company_name} · ${x.name} · ${money(x.spend)}`,href:"/dashboard/media/control-center",level:"warning"})),...(outstanding>0?[{title:"Prioritize collections",body:`${money(outstanding)} outstanding`,href:"/dashboard/clients/accounts-payment",level:"warning"}]:[])].slice(0,7),health=Math.max(0,100-Math.min(100,overdue.length*5+zero.length*8+riskByClient.filter(x=>x.score>=60).length*7));return <div className="vx-world vx-war-room" style={{display:"grid",gap:18}}><header className="vx-world-hero"><div><span className="eyebrow">EXECUTIVE</span><h1 className="page-title">Business Command Center</h1><p className="page-subtitle">Clients, delivery, media and collections in one decision view.</p></div><div className="vx-live-chip"><i/>HEALTH {health}/100</div></header><div className="vx-focus-strip"><span className="vx-focus-pill">{riskByClient.filter(x=>x.score>=40).length} clients need focus</span><span className="vx-focus-pill">{overdue.length} overdue</span><span className="vx-focus-pill">{zero.length} media anomalies</span><span className="vx-focus-pill">{review.length} review pressure</span></div><div className="grid grid-4"><div className="card"><small>Active clients</small><h2>{clients.length}</h2></div><div className="card"><small>Media spend MTD</small><h2>{money(spend)}</h2><p>Includes historical spend from archived campaigns</p></div><div className="card"><small>Tracked revenue</small><h2>{money(revenue)}</h2></div><div className="card"><small>Outstanding</small><h2>{money(outstanding)}</h2></div></div><div className="grid grid-2"><section className="card"><h2>Decisions</h2><p className="text-muted">Highest-priority actions from current operating data.</p><div className="vx-decision-stack" style={{marginTop:14}}>{decisions.length?decisions.map((x,i)=><Link className="vx-decision" href={x.href} key={i} style={{textDecoration:"none",color:"inherit"}}><span className="vx-decision-index">{String(i+1).padStart(2,"0")}</span><span><b>{x.title}</b><small style={{display:"block",color:"var(--text-muted)"}}>{x.body}</small></span><span className={`badge ${x.level==="critical"?"badge-red":"badge-amber"}`}>{x.level}</span></Link>):<div className="empty good">No critical decision is waiting.</div>}</div></section><section className="card"><h2>Client Sonar</h2><p className="text-muted">Delivery, media, revisions and collection pressure.</p><div style={{display:"grid",gap:10,marginTop:14}}>{riskByClient.slice(0,10).map(x=><div key={x.name} style={{display:"grid",gridTemplateColumns:"1fr auto",gap:10,paddingBottom:10,borderBottom:"1px solid var(--card-border)"}}><div><b>{x.name}</b><small style={{display:"block",color:"var(--text-muted)"}}>{x.od} overdue · {x.z} media · {x.rv} revisions · {money(x.b)} due</small></div><span className={`badge ${x.score>=60?"badge-red":x.score>=30?"badge-amber":"badge-green"}`}>{x.score}/100</span></div>)}</div></section></div></div>}
