import Link from "next/link";
import {requireGroupSuperAdmin} from "@/lib/vgroup/access";
import {getVGroupSql} from "@/lib/vgroup/db";
export const dynamic="force-dynamic";
const money=(v:number)=>new Intl.NumberFormat("en-EG",{style:"currency",currency:"EGP",maximumFractionDigits:0}).format(v||0);
export default async function CommandCenter(){await requireGroupSuperAdmin();const sql=getVGroupSql();const rows=await sql<any[]>`select business_unit_id::text,code,pending_approvals,open_exceptions,open_housekeeping,open_guest_complaints,open_support_tickets,open_sales_opportunities,month_net_movement from vgroup.executive_command_center order by code`;return <main style={{minHeight:"100vh",background:"#08090d",color:"#f5f7fb",padding:"32px 22px",fontFamily:"Inter,system-ui,sans-serif"}}><section style={{maxWidth:1180,margin:"0 auto"}}><Link href="/group" style={{color:"#9cc9ff",textDecoration:"none",fontWeight:800}}>← Group</Link><div style={{margin:"34px 0 26px"}}><div style={{fontSize:12,letterSpacing:".18em",fontWeight:900,color:"#9cc9ff"}}>EXECUTIVE GROUP COMMAND CENTER</div><h1 style={{fontSize:"clamp(36px,5vw,62px)",letterSpacing:"-.05em",margin:"10px 0"}}>Vivit Group pulse</h1><p style={{maxWidth:800,color:"#a5afbf",lineHeight:1.7}}>Cross-business operational exceptions, approvals, Hospitality service queues, Tech commercial/support queues and current-month ledger movement. Server-side summary only.</p></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(300px,1fr))",gap:16}}>{Array.from(rows).map((row:any)=><article key={row.business_unit_id} style={{padding:24,borderRadius:24,border:"1px solid #242a35",background:"linear-gradient(145deg,rgba(104,171,255,.09),rgba(255,255,255,.02))"}}><div style={{textTransform:"uppercase",fontSize:12,letterSpacing:".16em",color:"#9cc9ff",fontWeight:900}}>{row.code}</div><strong style={{display:"block",fontSize:30,margin:"12px 0"}}>{money(Number(row.month_net_movement||0))}</strong><div style={{display:"grid",gridTemplateColumns:"repeat(2,minmax(0,1fr))",gap:8,fontSize:13,color:"#bdc6d3"}}><span>Approvals <b>{row.pending_approvals}</b></span><span>Exceptions <b>{row.open_exceptions}</b></span><span>Housekeeping <b>{row.open_housekeeping}</b></span><span>Guest complaints <b>{row.open_guest_complaints}</b></span><span>Support tickets <b>{row.open_support_tickets}</b></span><span>Sales opps <b>{row.open_sales_opportunities}</b></span></div></article>)}</div></section></main>}
