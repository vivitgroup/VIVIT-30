"use client";
import Image from "next/image";
import Link from "next/link";
import { useSearchParams } from "next/navigation";
import { Suspense,useState } from "react";
function ResetForm(){const token=useSearchParams().get("token")||"";const [password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(password.length<8)return setError("Use at least 8 characters.");if(password!==confirm)return setError("Passwords do not match.");setLoading(true);const r=await fetch("/api/password/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password})});const d=await r.json();if(!r.ok)setError(d.error);else setDone(true);setLoading(false);}return <div className="auth-simple-card"><Image src="/vivit-logo.png" alt="VIVIT" width={190} height={105} style={{objectFit:"contain",margin:"0 auto 24px"}}/>{done?<><div className="auth-success">✓</div><h1>Password updated</h1><p>You can now sign in with your new password.</p><Link href="/login" className="auth-primary">Sign in</Link></>:<><h1>Create new password</h1><p>Choose a strong password for your VIVIT ERP account.</p>{error&&<div className="auth-error">{error}</div>}<form onSubmit={submit}><input className="vivit-input" type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><input className="vivit-input" type="password" required value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm password"/><button className="auth-primary" disabled={loading||!token}>{loading?"Updating…":"Update password →"}</button></form></>}</div>}
export default function ResetPassword(){return <main className="auth-simple"><Suspense><ResetForm/></Suspense></main>}
