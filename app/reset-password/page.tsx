"use client";
import Image from "next/image";
import Link from "next/link";
import { useSearchParams } from "next/navigation";
import { Suspense,useState } from "react";
const strong=(v:string)=>v.length>=12&&v.length<=128&&/[a-z]/.test(v)&&/[A-Z]/.test(v)&&/\d/.test(v)&&/[^A-Za-z0-9]/.test(v);
function ResetForm(){const token=useSearchParams().get("token")||"";const [password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(!strong(password))return setError("Use 12–128 characters with uppercase, lowercase, number, and symbol.");if(password!==confirm)return setError("Passwords do not match.");setLoading(true);setError("");try{const r=await fetch("/api/password/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password})});const d=await r.json();if(!r.ok)setError(d.error||"Password could not be reset.");else setDone(true);}finally{setLoading(false)}}return <div className="auth-simple-card"><Image src="/vivit-logo.png" alt="VIVIT" width={190} height={105} style={{objectFit:"contain",margin:"0 auto 24px"}}/>{done?<><div className="auth-success">✓</div><h1>Password updated</h1><p>You can now sign in with your new password.</p><Link href="/login" className="auth-primary">Sign in</Link></>:<><h1>Create new password</h1><p>Use 12–128 characters with uppercase, lowercase, a number, and a symbol.</p>{error&&<div className="auth-error">{error}</div>}<form onSubmit={submit}><input className="vivit-input" type="password" required minLength={12} maxLength={128} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><input className="vivit-input" type="password" required minLength={12} maxLength={128} autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm password"/><button className="auth-primary" disabled={loading||!token}>{loading?"Updating…":"Update password →"}</button></form></>}</div>}
export default function ResetPassword(){return <main className="auth-simple"><Suspense><ResetForm/></Suspense></main>}
