"use client";
import { usePathname } from "next/navigation";
import { useState } from "react";
export function ClientLogoManager({role}:{role:string}){const path=usePathname(),m=path.match(/^\/dashboard\/clients\/([^/]+)$/),[open,setOpen]=useState(false),[logo,setLogo]=useState(""),[busy,setBusy]=useState(false),[msg,setMsg]=useState("");if(!m||!["SUPER_ADMIN","ACCOUNT_MANAGER"].includes(role))return null;const id=m[1];async function save(){setBusy(true);setMsg("");try{const r=await fetch(`/api/clients/${id}/logo`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({logo})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Could not save logo");setMsg("Logo saved");setTimeout(()=>location.reload(),450)}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}return <><button onClick={()=>setOpen(true)} style={{position:"fixed",right:88,bottom:22,zIndex:880,height:42,padding:"0 13px",borderRadius:13,border:"1px solid var(--card-border)",background:"var(--card-bg)",boxShadow:"0 12px 30px rgba(15,23,42,.12)",fontWeight:800,fontSize:12,cursor:"pointer"}}>🖼 Client Logo</button>{open&&<div style={{position:"fixed",inset:0,zIndex:950,background:"rgba(15,23,42,.5)",backdropFilter:"blur(7px)",display:"grid",placeItems:"center",padding:18}} onClick={()=>setOpen(false)}><div onClick={e=>e.stopPropagation()} style={{width:"min(430px,96vw)",background:"var(--card-bg)",border:"1px solid var(--card-border)",borderRadius:20,padding:20,boxShadow:"0 30px 80px rgba(15,23,42,.25)"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><h3>Client Logo</h3><p style={{fontSize:11.5,color:"var(--text-muted)",marginTop:3}}>Paste an image URL for this client's portal identity.</p></div><button onClick={()=>setOpen(false)} className="btn btn-ghost btn-sm">×</button></div><label className="form-label" style={{marginTop:16}}>Logo image URL</label><input value={logo} onChange={e=>setLogo(e.target.value)} className="form-input" placeholder="https://.../logo.png"/><div style={{display:"flex",gap:8,marginTop:12}}><button onClick={save} disabled={busy} className="btn btn-primary" style={{flex:1}}>{busy?"Saving...":"Save Logo"}</button><button onClick={()=>{setLogo("");}} className="btn btn-secondary">Clear</button></div>{msg&&<p style={{fontSize:12,marginTop:9,color:msg==="Logo saved"?"var(--green)":"var(--red)"}}>{msg}</p>}</div></div>}</>}
