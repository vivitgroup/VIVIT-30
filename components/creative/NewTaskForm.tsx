"use client";
import {useMemo,useState} from "react";
import Link from "next/link";
import {createTask} from "@/lib/actions";
import {TASK_TEMPLATES} from "@/lib/task-templates";

type Option={id:string;name:string};type Client={id:string;companyName:string};type TaskTemplate=(typeof TASK_TEMPLATES)[number];
const types=["REEL","GRAPHIC","CAROUSEL","MOTION_GRAPHIC","VIDEO_EDIT","PHOTO_SESSION","STORY","UGC"];
const typeLabels:Record<string,string>={REEL:"🎬 Reel",GRAPHIC:"🎨 Graphic",CAROUSEL:"📊 Carousel",MOTION_GRAPHIC:"✨ Motion Graphic",VIDEO_EDIT:"🎥 Video Edit",PHOTO_SESSION:"📸 Photo Session",STORY:"📱 Story",UGC:"👤 UGC"};
const pad=(n:number)=>String(n).padStart(2,"0");
function toDisplay(iso:string){if(!iso)return"";const[y,m,d]=iso.split("-");return`${d}/${m}/${y}`}
function toIso(v:string){const m=v.match(/^(\d{2})\/(\d{2})\/(\d{4})$/);if(!m)return"";const[,d,mo,y]=m,n=new Date(Number(y),Number(mo)-1,Number(d));return n.getFullYear()===Number(y)&&n.getMonth()===Number(mo)-1&&n.getDate()===Number(d)?`${y}-${mo}-${d}`:""}
function addDays(days:number){const d=new Date();d.setDate(d.getDate()+days);return`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}
function formatDateInput(raw:string){const x=raw.replace(/\D/g,"").slice(0,8);return x.length<=2?x:x.length<=4?`${x.slice(0,2)}/${x.slice(2)}`:`${x.slice(0,2)}/${x.slice(2,4)}/${x.slice(4)}`}

export default function NewTaskForm({clients,creators}:{clients:Client[];creators:Option[]}){
 const[title,setTitle]=useState(""),[type,setType]=useState(""),[priority,setPriority]=useState("MEDIUM"),[brief,setBrief]=useState(""),[tov,setTov]=useState(""),[reference,setReference]=useState(""),[referenceKind,setReferenceKind]=useState<"LINK"|"IMAGE">("LINK"),[deadlineText,setDeadlineText]=useState(""),[activeTemplate,setActiveTemplate]=useState("");
 const deadline=useMemo(()=>toIso(deadlineText),[deadlineText]);
 function applyTemplate(t:TaskTemplate){setActiveTemplate(t.id);setTitle(String(t.name||"").replace(/^[^\s]+\s/,""));setType(t.type);setPriority(t.priority);setBrief(t.brief);setTov(t.tov||"");setDeadlineText(toDisplay(addDays(Number(t.daysToDeadline||3))));setTimeout(()=>document.getElementById("taskForm")?.scrollIntoView({behavior:"smooth",block:"start"}),50)}
 return <div className="animate-fade-up" style={{display:"grid",gap:18,maxWidth:980}}><div className="flex items-center gap-3"><Link href="/dashboard/creative" className="text-[#6B8FAF] text-xl">←</Link><div><h1 className="page-title">New Creative Task</h1><p className="page-subtitle">Create a clear task, attach a link or image reference, and set the deadline.</p></div></div><div className="card"><div className="card-header"><div><p className="card-title">⚡ Quick Templates</p><p className="page-subtitle" style={{margin:0}}>Use a template, then customize the task.</p></div></div><div className="card-body" style={{display:"flex",flexWrap:"wrap",gap:8}}>{TASK_TEMPLATES.map(t=><button key={t.id} type="button" onClick={()=>applyTemplate(t)} className={activeTemplate===t.id?"btn btn-primary btn-sm":"btn btn-ghost btn-sm"}>{t.name}</button>)}</div></div><form action={createTask} id="taskForm" style={{display:"grid",gap:18}}><input type="hidden" name="deadline" value={deadline}/><input type="hidden" name="referenceUrl" value={reference}/><input type="hidden" name="referenceKind" value={referenceKind}/><div className="card"><div className="card-header"><p className="card-title">Task Details</p></div><div className="card-body" style={{display:"grid",gap:14}}><label className="form-label">TASK TITLE *<input name="title" value={title} onChange={e=>setTitle(e.target.value)} required className="form-input"/></label><div className="form-grid"><label className="form-label">CLIENT *<select name="clientId" required className="form-select"><option value="">Select client…</option>{clients.map(c=><option key={c.id} value={c.id}>{c.companyName}</option>)}</select></label><label className="form-label">CREATIVE TYPE *<select name="type" required value={type} onChange={e=>setType(e.target.value)} className="form-select"><option value="">Select type…</option>{types.map(t=><option key={t} value={t}>{typeLabels[t]}</option>)}</select></label><label className="form-label">ASSIGN TO<select name="assignedToId" className="form-select"><option value="">Unassigned</option>{creators.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="form-label">PRIORITY *<select name="priority" value={priority} onChange={e=>setPriority(e.target.value)} required className="form-select"><option value="URGENT">Urgent</option><option value="HIGH">High</option><option value="MEDIUM">Medium</option><option value="LOW">Low</option></select></label></div><label className="form-label">DEADLINE * <small>DD/MM/YYYY</small><input value={deadlineText} onChange={e=>setDeadlineText(formatDateInput(e.target.value))} inputMode="numeric" maxLength={10} required className="form-input"/></label>{deadlineText&&!deadline&&<p className="form-error">Enter a valid date as DD/MM/YYYY.</p>}</div></div><div className="card"><div className="card-header"><p className="card-title">Creative Brief & Reference</p></div><div className="card-body" style={{display:"grid",gap:14}}><label className="form-label">BRIEF *<textarea name="brief" value={brief} onChange={e=>setBrief(e.target.value)} required rows={7} className="form-input"/></label><div className="form-grid"><label className="form-label">REFERENCE TYPE<select value={referenceKind} onChange={e=>setReferenceKind(e.target.value as "LINK"|"IMAGE")} className="form-select"><option value="LINK">Link</option><option value="IMAGE">Image URL</option></select></label><label className="form-label">REFERENCE {referenceKind==="IMAGE"?"IMAGE URL":"LINK"}<input value={reference} onChange={e=>setReference(e.target.value)} type="url" className="form-input" placeholder={referenceKind==="IMAGE"?"https://.../reference.jpg":"https://drive.google.com/..."}/></label></div><small className="page-subtitle">The Creator gets a visible “Reference” button inside the task. Image references open directly as the image URL.</small><label className="form-label">TONE OF VOICE<textarea name="tov" value={tov} onChange={e=>setTov(e.target.value)} rows={2} className="form-input"/></label><label className="form-label">CAPTION / COPY<textarea name="caption" rows={3} className="form-input"/></label></div></div><div style={{display:"flex",gap:10}}><Link href="/dashboard/creative" className="btn btn-ghost" style={{flex:1,justifyContent:"center"}}>Cancel</Link><button type="submit" className="btn btn-primary" disabled={!deadline} style={{flex:1,justifyContent:"center"}}>Create Task →</button></div></form></div>;
}
