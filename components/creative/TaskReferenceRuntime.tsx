"use client";
import {useEffect,useState} from "react";
import {usePathname} from "next/navigation";
type RefRow={id:string;kind:string;url:string;label?:string|null};
export function TaskReferenceRuntime(){const path=usePathname(),match=path.match(/^\/dashboard\/creative\/([^/]+)$/),taskId=match?.[1]||"",[refs,setRefs]=useState<RefRow[]>([]);useEffect(()=>{if(!taskId){setRefs([]);return}let live=true;fetch(`/api/task-references?taskId=${encodeURIComponent(taskId)}`,{cache:"no-store"}).then(async r=>r.ok?await r.json():{references:[]}).then(d=>{if(live)setRefs(Array.isArray(d.references)?d.references:[])}).catch(()=>{if(live)setRefs([])});return()=>{live=false}},[taskId]);if(!taskId||!refs.length)return null;const ref=refs[0];return <a href={ref.url} target="_blank" rel="noopener noreferrer" aria-label={`Open ${ref.kind.toLowerCase()} reference`} style={{position:"fixed",right:24,bottom:96,zIndex:1200,minHeight:48,display:"inline-flex",alignItems:"center",gap:8,padding:"0 16px",borderRadius:999,background:"var(--erp-accent,#0066ff)",color:"#fff",fontWeight:900,textDecoration:"none",boxShadow:"0 14px 36px rgba(15,23,42,.24)"}}>↗ Reference <span style={{fontSize:10,opacity:.8}}>{ref.kind}</span></a>}
