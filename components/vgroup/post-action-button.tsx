"use client";
import {useState} from "react";
export function PostActionButton({endpoint,label}:{endpoint:string;label:string}){const [busy,setBusy]=useState(false);const [msg,setMsg]=useState("");async function run(){setBusy(true);setMsg("");try{const res=await fetch(endpoint,{method:"POST",headers:{"content-type":"application/json"},body:"{}"});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||`Request failed (${res.status})`);setMsg("Done");window.location.reload()}catch(e){setMsg(e instanceof Error?e.message:"Action failed")}finally{setBusy(false)}}return <div style={{display:"flex",gap:8,alignItems:"center"}}><button disabled={busy} onClick={run} style={{padding:"8px 11px",borderRadius:9,border:0,fontWeight:900,cursor:"pointer"}}>{busy?"Working…":label}</button>{msg?<small>{msg}</small>:null}</div>}
