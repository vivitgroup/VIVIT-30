"use client";
import {useEffect,useState} from "react";

type Data={plans:any[];subscriptions:any[];invoices:any[];slaIncidents:any[]};
export function SaasPanel(){const [data,setData]=useState<Data>({plans:[],subscriptions:[],invoices:[],slaIncidents:[]});useEffect(()=>{fetch('/api/vgroup/tech/saas',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject()).then(setData).catch(()=>{})},[]);const card=(title:string,value:number)=><article style={{padding:18,border:'1px solid #193650',borderRadius:18,background:'rgba(46,168,255,.06)'}}><div style={{fontSize:12,color:'#9eb3c8'}}>{title}</div><b style={{fontSize:28}}>{value}</b></article>;return <div style={{display:'grid',gap:18}}><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:12}}>{card('Plans',data.plans.length)}{card('Subscriptions',data.subscriptions.length)}{card('Invoices',data.invoices.length)}{card('SLA incidents',data.slaIncidents.length)}</div><div style={{display:'grid',gap:10}}>{data.subscriptions.slice(0,25).map((s:any)=><article key={s.id} style={{padding:16,border:'1px solid #193650',borderRadius:16}}><b>{s.client_name} · {s.plan_name}</b><div style={{color:'#9eb3c8',marginTop:6}}>{s.status} · tenant {s.tenant_key}</div></article>)}</div></div>}
