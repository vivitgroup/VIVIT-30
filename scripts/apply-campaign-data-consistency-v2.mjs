import fs from "node:fs";
const read=p=>fs.readFileSync(p,"utf8"),write=(p,s)=>fs.writeFileSync(p,s),must=(ok,msg)=>{if(!ok)throw new Error(msg)};
const one=(p,a,b,msg)=>{let s=read(p);must(s.includes(a),msg);s=s.replace(a,b);write(p,s)};

one("app/dashboard/clients/[id]/page.tsx","today=iso(new Date()),defaultFrom=iso(new Date(Date.now()-30*86400000))","current=new Date(),today=iso(current),defaultFrom=iso(new Date(current.getTime()-30*86400000))","client date purity pattern missing");
one("app/dashboard/clients/[id]/page.tsx","where a.client_id=${id} and a.archived_at is null group by","where a.client_id=${id} and a.archived_at is null and a.deleted_at is null group by","client campaign lifecycle filter missing");
one("app/dashboard/portal/page.tsx","defaultFrom=iso(new Date(Date.now()-30*86400000))","defaultFrom=iso(new Date(now.getTime()-30*86400000))","portal date purity pattern missing");
{
 const p="app/dashboard/portal/page.tsx";let s=read(p);
 s=s.replace("c.archived_at is null group by","c.archived_at is null and c.deleted_at is null group by");
 s=s.replace("creative_tasks where id=${taskId} and client_id=${client.id} and workspace_id=${workspaceId} and archived_at is null limit 1","creative_tasks where id=${taskId} and client_id=${client.id} and workspace_id=${workspaceId} and archived_at is null and deleted_at is null limit 1");
 s=s.replace("creative_tasks where workspace_id=${workspaceId} and client_id=${client.id} and archived_at is null order by","creative_tasks where workspace_id=${workspaceId} and client_id=${client.id} and archived_at is null and deleted_at is null order by");
 s=s.replace("creative_tasks t where t.id=e.task_id and t.client_id=${client.id} and t.archived_at is null)","creative_tasks t where t.id=e.task_id and t.client_id=${client.id} and t.archived_at is null and t.deleted_at is null)");
 must(s.includes("c.archived_at is null and c.deleted_at is null"),"portal campaign lifecycle patch failed");write(p,s);
}
{
 const p="app/dashboard/page.tsx";let s=read(p),anchor='  const period   = `${year}-${String(month).padStart(2,"0")}`;';must(s.includes(anchor),"dashboard period anchor missing");
 s=s.replace(anchor,anchor+'\n  const activeTask=sql`${creativeTasks.id} in (select id from creative_tasks where workspace_id=${workspaceId} and archived_at is null and deleted_at is null)`;\n  const activeLead=sql`${salesLeads.id} in (select id from sales_leads where workspace_id=${workspaceId} and archived_at is null and deleted_at is null)`;');
 s=s.replaceAll('and(eq(creativeTasks.workspaceId,workspaceId),notInArray(creativeTasks.status,["COMPLETED","REJECTED"]))','and(eq(creativeTasks.workspaceId,workspaceId),notInArray(creativeTasks.status,["COMPLETED","REJECTED"]),activeTask)');
 s=s.replace('and(eq(creativeTasks.workspaceId,workspaceId),eq(creativeTasks.status,"REVIEW"))','and(eq(creativeTasks.workspaceId,workspaceId),eq(creativeTasks.status,"REVIEW"),activeTask)');
 s=s.replace('and(eq(creativeTasks.workspaceId,workspaceId),lt(creativeTasks.deadline,today),notInArray(creativeTasks.status,["COMPLETED","REJECTED","APPROVED"]))','and(eq(creativeTasks.workspaceId,workspaceId),lt(creativeTasks.deadline,today),notInArray(creativeTasks.status,["COMPLETED","REJECTED","APPROVED"]),activeTask)');
 s=s.replace('and(eq(salesLeads.workspaceId,workspaceId),eq(salesLeads.stage,"WON"))','and(eq(salesLeads.workspaceId,workspaceId),eq(salesLeads.stage,"WON"),activeLead)');
 s=s.replace('and(eq(salesLeads.workspaceId,workspaceId),lte(salesLeads.updatedAt,new Date(today.getTime()-5*86400000)),notInArray(salesLeads.stage,["WON","LOST"]))','and(eq(salesLeads.workspaceId,workspaceId),lte(salesLeads.updatedAt,new Date(today.getTime()-5*86400000)),notInArray(salesLeads.stage,["WON","LOST"]),activeLead)');write(p,s);
}
{
 const p="app/api/media-control-v2/route.ts";let s=read(p);s=s.replace("where archived_at is null)`","where archived_at is null and deleted_at is null)`");s=s.replace("where archived_at is not null)`","where archived_at is not null and deleted_at is null)`");must(s.includes("archived_at is null and deleted_at is null"),"media lifecycle patch failed");write(p,s);
}
one("app/api/assistant/route.ts","and ac.archived_at is null\n group by","and ac.archived_at is null and ac.deleted_at is null\n group by","Vivito campaign filter missing");
{
 const p="app/dashboard/analytics/page.tsx";let s=read(p);s=s.replace("creativeTasks,mediaMetrics,salesLeads","creativeTasks,salesLeads");s=s.replace("archived_at is null)`","archived_at is null and deleted_at is null)`");
 const old="db.select({spend:sum(mediaMetrics.adSpend),revenue:sum(mediaMetrics.revenue),leads:sum(mediaMetrics.leads),impressions:sum(mediaMetrics.impressions),clicks:sum(mediaMetrics.clicks)}).from(mediaMetrics).where(and(eq(mediaMetrics.workspaceId,workspaceId),gte(mediaMetrics.date,monthStart)))";
 const neu="db.execute<{spend:number|string|null;revenue:number|string|null;leads:number|string|null;impressions:number|string|null;clicks:number|string|null}>(sql`select coalesce(sum(p.spend),0) spend,coalesce(sum(p.revenue),0) revenue,coalesce(sum(p.results),0) leads,coalesce(sum(p.impressions),0) impressions,coalesce(sum(p.clicks),0) clicks from ad_performance_daily p join ad_campaigns a on a.id=p.campaign_id where a.workspace_id=${workspaceId} and a.archived_at is null and a.deleted_at is null and p.date>=${monthStart} and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null`).then(r=>Array.from(r))";must(s.includes(old),"analytics legacy query missing");write(p,s.replace(old,neu));
}
{
 const p="app/api/monthly-summary/[clientId]/route.ts";let s=read(p);s=s.replace("db,clients,mediaMetrics,creativeTasks","db,clients,creativeTasks");
 const old="db.select().from(mediaMetrics).where(and(eq(mediaMetrics.workspaceId,workspaceId),eq(mediaMetrics.clientId,clientId),gte(mediaMetrics.date,monthStart),lte(mediaMetrics.date,monthEnd)))";
 const neu="db.execute<{platform:string;adSpend:number|string|null;leads:number|string|null;purchases:number|string|null;revenue:number|string|null}>(sql`select a.platform,coalesce(sum(p.spend),0) \"adSpend\",coalesce(sum(p.results),0) leads,coalesce(sum(p.purchases),0) purchases,coalesce(sum(p.revenue),0) revenue from ad_performance_daily p join ad_campaigns a on a.id=p.campaign_id where a.workspace_id=${workspaceId} and a.client_id=${clientId} and a.archived_at is null and a.deleted_at is null and p.date>=${monthStart} and p.date<=${monthEnd} and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null group by a.platform`).then(r=>Array.from(r))";must(s.includes(old),"monthly summary legacy query missing");write(p,s.replace(old,neu));
}
{
 const p="app/api/pdf-report/[clientId]/route.ts";let s=read(p);s=s.replace("db,clients,financeRecords,mediaMetrics,contacts,workspaces","db,clients,financeRecords,contacts,workspaces,sql");s=s.replace("eq,and,gte,lte,sum","eq,and");
 const old="db.select({spend:sum(mediaMetrics.adSpend),leads:sum(mediaMetrics.leads),revenue:sum(mediaMetrics.revenue)}).from(mediaMetrics).where(and(eq(mediaMetrics.workspaceId,workspaceId),eq(mediaMetrics.clientId,clientId),gte(mediaMetrics.date,monthStart),lte(mediaMetrics.date,monthEnd)))";
 const neu="db.execute<{spend:number|string|null;leads:number|string|null;revenue:number|string|null}>(sql`select coalesce(sum(p.spend),0) spend,coalesce(sum(p.results),0) leads,coalesce(sum(p.revenue),0) revenue from ad_performance_daily p join ad_campaigns a on a.id=p.campaign_id where a.workspace_id=${workspaceId} and a.client_id=${clientId} and a.archived_at is null and a.deleted_at is null and p.date>=${monthStart} and p.date<=${monthEnd} and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null`).then(r=>Array.from(r))";must(s.includes(old),"pdf legacy query missing");write(p,s.replace(old,neu));
}
{
 const p="app/api/performance-score/route.ts";let s=read(p);s=s.replace("clientFeedback,mediaMetrics,users","clientFeedback,users");s=s.replace("eq,and,gte,lt,sum,count,avg,desc","eq,and,gte,lt,sum,count,desc");
 const old="const[roasAgg]=await db.select({avgRoas:avg(mediaMetrics.roas)}).from(mediaMetrics).where(and(eq(mediaMetrics.workspaceId,workspaceId),eq(mediaMetrics.clientId,clientId),gte(mediaMetrics.date,mo3ago)));";
 const neu="const[roasAgg]=Array.from(await db.execute<{spend:number|string|null;revenue:number|string|null}>(sql`select coalesce(sum(p.spend),0) spend,coalesce(sum(p.revenue),0) revenue from ad_performance_daily p join ad_campaigns a on a.id=p.campaign_id where a.workspace_id=${workspaceId} and a.client_id=${clientId} and a.archived_at is null and a.deleted_at is null and p.date>=${mo3ago} and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null`));";must(s.includes(old),"health legacy query missing");s=s.replace(old,neu);s=s.replace("roas=Number(roasAgg?.avgRoas??2.5),roasScore=Math.min(100,Math.round((roas/3)*100))","mediaSpend=Number(roasAgg?.spend||0),mediaRevenue=Number(roasAgg?.revenue||0),roas=mediaSpend>0?mediaRevenue/mediaSpend:0,roasScore=mediaSpend>0?Math.min(100,Math.round((roas/3)*100)):75");write(p,s);
}
write("app/api/v1/metrics/route.ts",`export const dynamic="force-dynamic";\nimport {NextRequest,NextResponse} from "next/server";\nimport {db,apiKeys,sql} from "@/lib/db";\nimport {eq,and} from "drizzle-orm";\nimport crypto from "crypto";\nconst READ_PERMISSIONS=new Set(["read","read_write","admin"]);\nasync function authenticateAPIKey(req:NextRequest){const raw=req.headers.get("x-api-key")??req.headers.get("authorization")?.replace(/^Bearer\\s+/i,"");if(!raw)return null;const keyHash=crypto.createHash("sha256").update(raw).digest("hex");const [key]=await db.select().from(apiKeys).where(and(eq(apiKeys.keyHash,keyHash),eq(apiKeys.isActive,true))).limit(1);if(!key||!READ_PERMISSIONS.has(String(key.permissions||"")))return null;await db.update(apiKeys).set({lastUsedAt:new Date()}).where(eq(apiKeys.id,key.id));return key}\ntype MetricRow={platform:string;adSpend:number|string|null;leads:number|string|null;revenue:number|string|null};\nexport async function GET(req:NextRequest){const key=await authenticateAPIKey(req);if(!key)return NextResponse.json({error:"Invalid API key"},{status:401});const now=new Date(),monthStart=new Date(now.getFullYear(),now.getMonth(),1),rows=Array.from(await db.execute<MetricRow>(sql\`select a.platform,coalesce(sum(p.spend),0) "adSpend",coalesce(sum(p.results),0) leads,coalesce(sum(p.revenue),0) revenue from ad_performance_daily p join ad_campaigns a on a.id=p.campaign_id join clients c on c.id=a.client_id where a.workspace_id=\${key.workspaceId} and c.workspace_id=\${key.workspaceId} and c.is_active=true and a.archived_at is null and a.deleted_at is null and p.date>=\${monthStart} and p.breakdown_type='TOTAL' and p.ad_set_id is null and p.ad_id is null group by a.platform\`)),data=rows.map(row=>{const adSpend=Number(row.adSpend||0),revenue=Number(row.revenue||0);return{platform:row.platform,adSpend,leads:Number(row.leads||0),revenue,roas:adSpend?revenue/adSpend:0}});return NextResponse.json({data,period:"MTD",source:"ad_performance_daily:TOTAL"},{headers:{"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}\n`);
{
 const p="app/dashboard/monthly-reports/page.tsx";let s=read(p);s=s.replace('type SessionUser={role?:Role|string;id?:string};','type SessionUser={role?:Role|string;id?:string;workspaceId?:string};');s=s.replace('const userId=String(sessionUser.id||"");','const userId=String(sessionUser.id||""),workspaceId=String(sessionUser.workspaceId||"");if(!workspaceId)redirect("/login");');s=s.replace('? and(eq(clients.isActive, true), eq(clients.accountManagerId, userId))\n      : eq(clients.isActive, true))','? and(eq(clients.workspaceId,workspaceId),eq(clients.isActive, true), eq(clients.accountManagerId, userId))\n      : and(eq(clients.workspaceId,workspaceId),eq(clients.isActive, true)))');s=s.replace('const {db,clients,contacts,financeRecords,mediaMetrics}=await import("@/lib/db");','const {db,clients,contacts,financeRecords,sql}=await import("@/lib/db");');s=s.replace('const {eq,and,gte,sum}=await import("drizzle-orm");','const {eq,and}=await import("drizzle-orm");');s=s.replace('if(!current?.user||![Role.SUPER_ADMIN,Role.ACCOUNT_MANAGER].includes(currentRole!))throw new Error("Unauthorized");','if(!current?.user||![Role.SUPER_ADMIN,Role.ACCOUNT_MANAGER].includes(currentRole!))throw new Error("Unauthorized");const currentWorkspaceId=String(currentUser?.workspaceId||"");if(!currentWorkspaceId)throw new Error("Workspace unavailable");');s=s.replace('? and(eq(clients.isActive,true),eq(clients.accountManagerId,currentUserId))\n      : eq(clients.isActive,true));','? and(eq(clients.workspaceId,currentWorkspaceId),eq(clients.isActive,true),eq(clients.accountManagerId,currentUserId))\n      : and(eq(clients.workspaceId,currentWorkspaceId),eq(clients.isActive,true)));');s=s.replace('db.select().from(financeRecords).where(and(eq(financeRecords.clientId,c.id),eq(financeRecords.month,pMonth),eq(financeRecords.year,pYear)))','db.select().from(financeRecords).where(and(eq(financeRecords.workspaceId,currentWorkspaceId),eq(financeRecords.clientId,c.id),eq(financeRecords.month,pMonth),eq(financeRecords.year,pYear)))');
 const old='db.select({spend:sum(mediaMetrics.adSpend),leads:sum(mediaMetrics.leads),rev:sum(mediaMetrics.revenue)}).from(mediaMetrics).where(and(eq(mediaMetrics.clientId,c.id),gte(mediaMetrics.date,monthStart2)))';const neu='db.execute<{spend:number|string|null;leads:number|string|null;rev:number|string|null}>(sql`select coalesce(sum(p.spend),0) spend,coalesce(sum(p.results),0) leads,coalesce(sum(p.revenue),0) rev from ad_performance_daily p join ad_campaigns a on a.id=p.campaign_id where a.workspace_id=${currentWorkspaceId} and a.client_id=${c.id} and a.archived_at is null and a.deleted_at is null and p.date>=${monthStart2} and p.date<${new Date(pYear,pMonth,1)} and p.breakdown_type=\'TOTAL\' and p.ad_set_id is null and p.ad_id is null`).then(r=>Array.from(r))';must(s.includes(old),"monthly bulk email legacy query missing");write(p,s.replace(old,neu));
}
const qa=`import fs from "node:fs";\nconst read=p=>fs.readFileSync(p,"utf8"),checks=[],check=(n,o)=>checks.push({n,o:Boolean(o)});\nconst client=read("app/dashboard/clients/[id]/page.tsx"),portal=read("app/dashboard/portal/page.tsx"),dash=read("app/dashboard/page.tsx"),media=read("app/api/media-control-v2/route.ts"),analytics=read("app/dashboard/analytics/page.tsx"),monthly=read("app/api/monthly-summary/[clientId]/route.ts"),pdf=read("app/api/pdf-report/[clientId]/route.ts"),monthlyPage=read("app/dashboard/monthly-reports/page.tsx"),v1=read("app/api/v1/metrics/route.ts"),health=read("app/api/performance-score/route.ts"),assistant=read("app/api/assistant/route.ts");\ncheck("Client selectable ranges",client.includes('name="from"')&&portal.includes('name="from"'));\ncheck("Client campaign lifecycle",client.includes("a.deleted_at is null")&&portal.includes("c.deleted_at is null"));\ncheck("Main dashboard lifecycle safe",dash.includes("activeTask")&&dash.includes("activeLead")&&dash.includes("archived_at is null and deleted_at is null"));\ncheck("Analytics canonical",!analytics.includes("mediaMetrics")&&analytics.includes("ad_performance_daily")&&analytics.includes("breakdown_type='TOTAL'"));\ncheck("Monthly canonical",!monthly.includes("mediaMetrics")&&monthly.includes("ad_performance_daily"));\ncheck("PDF canonical",!pdf.includes("mediaMetrics")&&pdf.includes("ad_performance_daily"));\ncheck("Bulk report scoped",!monthlyPage.includes("mediaMetrics")&&monthlyPage.includes("currentWorkspaceId")&&monthlyPage.includes("clients.workspaceId"));\ncheck("API weighted ROAS",!v1.includes("mediaMetrics")&&v1.includes("revenue/adSpend")&&v1.includes("ad_performance_daily"));\ncheck("Health no fake ROAS",!health.includes("mediaMetrics")&&!health.includes("avgRoas??2.5")&&health.includes("ad_performance_daily"));\ncheck("Vivito lifecycle",assistant.includes("ac.archived_at is null and ac.deleted_at is null"));\ncheck("Media lifecycle",media.includes("archived_at is null and deleted_at is null"));\nconst failed=checks.filter(item=>!item.o);for(const item of checks)console.log((item.o?"PASS":"FAIL")+"  "+item.n);console.log("\\n"+(checks.length-failed.length)+"/"+checks.length+" campaign data consistency checks passed.");if(failed.length)process.exit(1);\n`;
write("scripts/qa-campaign-data-consistency.mjs",qa);
console.log("Campaign KPI consistency remediation applied.");
