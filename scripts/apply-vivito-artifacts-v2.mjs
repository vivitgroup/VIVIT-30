import fs from "node:fs";

function replaceOnce(src,needle,replacement,label){if(src.includes(replacement))return src;if(!src.includes(needle))throw new Error(`Patch anchor missing: ${label}`);return src.replace(needle,replacement)}

const routePath="app/api/assistant/route.ts";let route=fs.readFileSync(routePath,"utf8");
route=replaceOnce(route,
'import {buildVivitoMemoryPlannerSystem,forgetVivitoMemory,likelyVivitoMemoryIntent,loadVivitoMemories,memoryContext,parseVivitoMemoryPlan,saveVivitoMemory} from "@/lib/vivito/memory";',
'import {buildVivitoMemoryPlannerSystem,forgetVivitoMemory,likelyVivitoMemoryIntent,loadVivitoMemories,memoryContext,parseVivitoMemoryPlan,saveVivitoMemory} from "@/lib/vivito/memory";\nimport {analyzeVivitoImage,groundedVivitoResearch} from "@/lib/vivito/multimodal";\nimport {buildVivitoArtifactPlannerSystem,likelyVivitoArtifactIntent,likelyVivitoResearchIntent,likelyVivitoVisionIntent,parseVivitoArtifactProposal,requestedArtifactKind} from "@/lib/vivito/artifact-router";',"route imports");

const helper=`async function readUploadedImageForVivito(userId:string,attachment:any){
 const found=rows(await db.execute(sql\`select storage_path,mime_type,size_bytes,name from file_documents where id=\${String(attachment?.fileId||"")} and workspace_id=\${W} and uploaded_by=\${userId} and archived_at is null and mime_type like 'image/%' limit 1\`));const row=found[0];if(!row)throw new Error("image-not-accessible");if(Number(row.size_bytes||0)>12*1024*1024)throw new Error("image-too-large-for-vision");const base=String(process.env.SUPABASE_URL||"").replace(/\\/$/,"");const key=String(process.env.SUPABASE_SERVICE_KEY||"");if(!base||!key)throw new Error("storage-not-configured");const r=await fetch(\`${base}/storage/v1/object/vivit-files/\${row.storage_path}\`,{headers:{apikey:key,Authorization:\`Bearer \${key}\`},signal:AbortSignal.timeout(20000)});if(!r.ok)throw new Error("image-fetch-failed");const bytes=Buffer.from(await r.arrayBuffer());return{mimeType:String(row.mime_type||attachment?.mimeType||"image/png"),base64:bytes.toString("base64"),name:String(row.name||attachment?.name||"image")}
}
`;
route=replaceOnce(route,"export async function POST(req:NextRequest){",helper+"\nexport async function POST(req:NextRequest){","image loader helper");

const routing=` if(likelyVivitoArtifactIntent(question)){
  try{const kind=requestedArtifactKind(question);let research:any=null,visionText="";if(likelyVivitoResearchIntent(question)){try{research=await groundedVivitoResearch(question)}catch{research={text:"Current web research was unavailable; do not invent current facts.",queries:[],sources:[]}}}const image=attachments.find((a:any)=>String(a.mimeType||"").startsWith("image/"));if(image){try{const input=await readUploadedImageForVivito(userId,image),vision=await analyzeVivitoImage({mimeType:input.mimeType,base64:input.base64,prompt:`Analyze this reference for the requested artifact: ${question}`});visionText=vision.text}catch{}}const evidence=[research?.text?`CURRENT RESEARCH:\\n${research.text}`:"",visionText?`IMAGE ANALYSIS:\\n${visionText}`:""].filter(Boolean).join("\\n\\n");const planned=await generateVivito(`USER REQUEST:\\n${question}\\n\\nAUTHORIZED CLIENTS:\\n${JSON.stringify(clients.map((c:any)=>c.company_name))}${evidence?`\\n\\nEVIDENCE:\\n${evidence}`:""}`,buildVivitoArtifactPlannerSystem(kind),{temperature:.08,maxTokens:6500}),artifactProposal=parseVivitoArtifactProposal(planned.text,kind);if(artifactProposal){const sources=["VIVITO Artifact Architect"];if(research?.sources?.length)sources.push(...research.sources.map((s:any)=>s.title).slice(0,8));if(visionText)sources.push("VIVITO Visual Intelligence");return NextResponse.json({answer:isArabic(question)?`جهزت هيكل ${artifactProposal.title}. تقدر تولّد الملف من الكارت تحت.`:`${artifactProposal.title} is structured and ready to generate.`,mode:"artifact-proposal",intelligence:"VIVITO",artifactProposal,sources,intelligenceMeta:{provider:planned.provider,artifactKind:kind,groundedResearch:!!research?.sources?.length,visionUsed:!!visionText}},{headers:{"Cache-Control":"private, no-store"}})}}catch(error){console.error("VIVITO artifact planning failed",error)}
 }
 if(likelyVivitoVisionIntent(question,attachments)){
  try{const image=attachments.find((a:any)=>String(a.mimeType||"").startsWith("image/"));if(image){const input=await readUploadedImageForVivito(userId,image),vision=await analyzeVivitoImage({mimeType:input.mimeType,base64:input.base64,prompt:question});return NextResponse.json({answer:vision.text,sources:[`Visual: ${input.name}`,"VIVITO Visual Intelligence"],mode:"vision",intelligence:"VIVITO",intelligenceMeta:{vision:true,model:vision.model}},{headers:{"Cache-Control":"private, no-store"}})}}catch(error){return NextResponse.json({answer:isArabic(question)?"مش قادر أقرأ الصورة دي بأمان دلوقتي. تأكد إنها PNG/JPG/WebP وأقل من 12MB.":"I could not analyze that image safely. Use PNG/JPG/WebP under 12MB.",mode:"vision-error",intelligence:"VIVITO"},{headers:{"Cache-Control":"private, no-store"}})}
 }
 if(likelyVivitoResearchIntent(question)){
  try{const research=await groundedVivitoResearch(question);return NextResponse.json({answer:research.text,sources:research.sources.map(s=>s.title).slice(0,12),sourceLinks:research.sources.slice(0,12),mode:"grounded-research",intelligence:"VIVITO",intelligenceMeta:{groundedResearch:true,queries:research.queries}},{headers:{"Cache-Control":"private, no-store"}})}catch{}
 }

`;
route=replaceOnce(route," if(likelyVivitoMemoryIntent(question)){",routing+" if(likelyVivitoMemoryIntent(question)){","automatic routing");
fs.writeFileSync(routePath,route);

const uiPath="components/assistant/SystemAssistant.tsx";let ui=fs.readFileSync(uiPath,"utf8");
ui=replaceOnce(ui,
"type ActionPlan={summary:string;steps:ActionProposal[];risk:Risk;requiresConfirmation:true;missingFields:string[]};\ntype RunState=\"ready\"|\"running\"|\"done\"|\"failed\";\ntype ChatMsg={id:string;who:\"you\"|\"vivito\";text:string;sources?:string[];action?:ActionProposal;plan?:ActionPlan;actionRequestId?:string;actionState?:RunState;actionResult?:string;planRequestId?:string;planState?:RunState;planResult?:string};",
"type ActionPlan={summary:string;steps:ActionProposal[];risk:Risk;requiresConfirmation:true;missingFields:string[]};\ntype ArtifactProposal={kind:\"pdf\"|\"xlsx\"|\"content-plan\";title:string;fileName:string;summary:string;pdf?:any;workbook?:any;contentPlan?:any};\ntype RunState=\"ready\"|\"running\"|\"done\"|\"failed\";\ntype ChatMsg={id:string;who:\"you\"|\"vivito\";text:string;sources?:string[];action?:ActionProposal;plan?:ActionPlan;artifact?:ArtifactProposal;artifactState?:RunState;artifactResult?:string;actionRequestId?:string;actionState?:RunState;actionResult?:string;planRequestId?:string;planState?:RunState;planResult?:string};","UI artifact types");

const artifactFn=` async function generateArtifact(messageId:string,a:ArtifactProposal){setMsgs(m=>m.map(x=>x.id===messageId?{...x,artifactState:"running"}:x));const isPdf=a.kind==="pdf",preview=isPdf?window.open("about:blank","_blank"):null;try{const endpoint=isPdf?"/api/assistant/artifacts/pdf":"/api/assistant/artifacts/xlsx",payload=isPdf?{spec:a.pdf,fileName:a.fileName}:a.kind==="content-plan"?{contentPlan:a.contentPlan,fileName:a.fileName}:{workbook:a.workbook,fileName:a.fileName};const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload),cache:"no-store"});if(!r.ok){const d=await r.json().catch(()=>({}));throw new Error(d.error||"Artifact generation failed")};const ct=String(r.headers.get("content-type")||"");if(isPdf&&ct.includes("text/html")){const html=await r.text();if(!preview)throw new Error("Popup was blocked. Allow popups to print this PDF.");preview.document.open();preview.document.write(html);preview.document.close();setTimeout(()=>{preview.focus();preview.print()},450);setMsgs(m=>m.map(x=>x.id===messageId?{...x,artifactState:"done",artifactResult:"Print-ready PDF opened with professional RTL/browser rendering."}:x));return}preview?.close();const blob=await r.blob(),url=URL.createObjectURL(blob),link=document.createElement("a");link.href=url;link.download=`${a.fileName||"vivito-artifact"}.${isPdf?"pdf":"xlsx"}`;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1500);setMsgs(m=>m.map(x=>x.id===messageId?{...x,artifactState:"done",artifactResult:`${isPdf?"PDF":"Excel workbook"} generated.`}:x))}catch(e:any){preview?.close();setMsgs(m=>m.map(x=>x.id===messageId?{...x,artifactState:"failed",artifactResult:e?.message||"Artifact generation failed safely."}:x))}}

`;
ui=replaceOnce(ui," async function send(text=q){",artifactFn+" async function send(text=q){","UI artifact generator");
ui=replaceOnce(ui,
"const msg:ChatMsg={id:botId,who:\"vivito\",text:d.answer||d.error||\"Connection interrupted. Try again.\",sources:Array.isArray(d.sources)?d.sources:[],action:d.actionProposal,plan:d.actionPlan,actionState:d.actionProposal?\"ready\":undefined,planState:d.actionPlan?\"ready\":undefined};",
"const msg:ChatMsg={id:botId,who:\"vivito\",text:d.answer||d.error||\"Connection interrupted. Try again.\",sources:Array.isArray(d.sources)?d.sources:[],action:d.actionProposal,plan:d.actionPlan,artifact:d.artifactProposal,artifactState:d.artifactProposal?\"ready\":undefined,actionState:d.actionProposal?\"ready\":undefined,planState:d.actionPlan?\"ready\":undefined};","UI response artifact");
const artifactCard=`{m.artifact&&<div className="va-action"><div className="va-action-top"><b>{m.artifact.kind==="pdf"?"PROFESSIONAL PDF":m.artifact.kind==="content-plan"?"CONTENT PLAN · XLSX":"EXCEL WORKBOOK"}</b><span>artifact</span></div><div>{m.artifact.summary||m.artifact.title}</div>{m.artifactState==="running"?<button disabled>Generating…</button>:m.artifactState==="done"?<div className="va-action-ok">✓ {m.artifactResult}</div>:m.artifactState==="failed"?<><div className="va-action-error">{m.artifactResult}</div><button onClick={()=>generateArtifact(m.id,m.artifact!)}>Retry generation</button></>:<button onClick={()=>generateArtifact(m.id,m.artifact!)}>Generate {m.artifact.kind==="pdf"?"PDF":"Excel"}</button>}</div>}\n    `;
ui=replaceOnce(ui,"{m.plan&&<div",artifactCard+"{m.plan&&<div","UI artifact card");
fs.writeFileSync(uiPath,ui);

const pkgPath="package.json";const pkg=JSON.parse(fs.readFileSync(pkgPath,"utf8"));if(!pkg.scripts["qa:vivito-artifacts-v2"])pkg.scripts["qa:vivito-artifacts-v2"]="tsx scripts/qa-vivito-artifacts-v2.ts";if(!pkg.scripts.build.includes("qa:vivito-artifacts-v2"))pkg.scripts.build=pkg.scripts.build.replace("npm run qa:vivito-creative-artifacts &&","npm run qa:vivito-creative-artifacts && npm run qa:vivito-artifacts-v2 &&");fs.writeFileSync(pkgPath,JSON.stringify(pkg,null,2)+"\n");
console.log("VIVITO artifact v2 integration patch applied.");
