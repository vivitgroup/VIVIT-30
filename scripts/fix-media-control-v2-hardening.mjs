import fs from "node:fs";
const path="app/api/media-control-v2/route.ts";
let s=fs.readFileSync(path,"utf8");
const replace=(from,to,label)=>{if(s.includes(to))return;if(!s.includes(from))throw new Error(`Missing anchor: ${label}`);s=s.replace(from,to)};

replace(
'async function persistDays(campaign:typeof adCampaigns.$inferSelect,days:UnifiedDay[]){for(const day of days||[]){if(!validDate(String(day.date||"")))continue;const date=new Date(`${day.date}T00:00:00Z`),spend=n(day.spend),results=n(day.results),purchases=n(day.purchases),impressions=n(day.impressions),clicks=n(day.clicks),revenue=n(day.revenue),reach=n(day.reach),frequency=n(day.frequency),addToCart=Math.max(0,Math.round(n(day.addToCart))),ctr=n(day.ctr)||(impressions?clicks/impressions*100:0),cpc=n(day.cpc)||(clicks?spend/clicks:0),cpm=n(day.cpm)||(impressions?spend/impressions*1000:0),costPerResult=n(day.costPerResult)||(results?spend/results:0),roas=spend?revenue/spend:0;await db.delete(adPerformanceDaily).where(and(eq(adPerformanceDaily.campaignId,campaign.id),eq(adPerformanceDaily.date,date),eq(adPerformanceDaily.breakdownType,"TOTAL"),isNull(adPerformanceDaily.adSetId),isNull(adPerformanceDaily.adId)));await db.execute(sql`insert into ad_performance_daily(id,campaign_id,date,breakdown_type,breakdown_value,spend,impressions,reach,clicks,results,qualified_leads,purchases,add_to_cart,revenue,frequency,ctr,cpc,cpm,cpl,cost_per_result,cpa,roas) values(gen_random_uuid()::text,${campaign.id},${date}::timestamp,\'TOTAL\',\'ALL\',${spend},${impressions},${reach},${clicks},${results},0,${purchases},${addToCart},${revenue},${frequency},${ctr},${cpc},${cpm},${costPerResult},${costPerResult},${purchases?spend/purchases:0},${roas})`)}}',
'async function persistDays(campaign:typeof adCampaigns.$inferSelect,days:UnifiedDay[]){await db.transaction(async tx=>{await tx.execute(sql`select pg_advisory_xact_lock(hashtext(${`media-v2-days:${campaign.id}`}))`);for(const day of days||[]){if(!validDate(String(day.date||"")))continue;const date=new Date(`${day.date}T00:00:00Z`),spend=n(day.spend),results=n(day.results),purchases=n(day.purchases),impressions=n(day.impressions),clicks=n(day.clicks),revenue=n(day.revenue),reach=n(day.reach),frequency=n(day.frequency),addToCart=Math.max(0,Math.round(n(day.addToCart))),ctr=n(day.ctr)||(impressions?clicks/impressions*100:0),cpc=n(day.cpc)||(clicks?spend/clicks:0),cpm=n(day.cpm)||(impressions?spend/impressions*1000:0),costPerResult=n(day.costPerResult)||(results?spend/results:0),roas=spend?revenue/spend:0;await tx.delete(adPerformanceDaily).where(and(eq(adPerformanceDaily.campaignId,campaign.id),eq(adPerformanceDaily.date,date),eq(adPerformanceDaily.breakdownType,"TOTAL"),isNull(adPerformanceDaily.adSetId),isNull(adPerformanceDaily.adId)));await tx.execute(sql`insert into ad_performance_daily(id,campaign_id,date,breakdown_type,breakdown_value,spend,impressions,reach,clicks,results,qualified_leads,purchases,add_to_cart,revenue,frequency,ctr,cpc,cpm,cpl,cost_per_result,cpa,roas) values(gen_random_uuid()::text,${campaign.id},${date}::timestamp,\'TOTAL\',\'ALL\',${spend},${impressions},${reach},${clicks},${results},0,${purchases},${addToCart},${revenue},${frequency},${ctr},${cpc},${cpm},${costPerResult},${costPerResult},${purchases?spend/purchases:0},${roas})`)}})}',
'persistDays transaction');

replace('db.select().from(adPlatformConnections).where(inArray(adPlatformConnections.clientId,c.ids))','db.select().from(adPlatformConnections).where(and(eq(adPlatformConnections.workspaceId,c.workspaceId),inArray(adPlatformConnections.clientId,c.ids)))','GET connection workspace');
replace('db.select().from(adCampaigns).where(and(inArray(adCampaigns.clientId,c.ids),sql`${adCampaigns.id} in (select id from ad_campaigns where archived_at is null)`))','db.select().from(adCampaigns).where(and(eq(adCampaigns.workspaceId,c.workspaceId),inArray(adCampaigns.clientId,c.ids),sql`${adCampaigns.id} in (select id from ad_campaigns where workspace_id=${c.workspaceId} and archived_at is null)`))','GET active workspace');
replace('db.select().from(adCampaigns).where(and(inArray(adCampaigns.clientId,c.ids),sql`${adCampaigns.id} in (select id from ad_campaigns where archived_at is not null)`))','db.select().from(adCampaigns).where(and(eq(adCampaigns.workspaceId,c.workspaceId),inArray(adCampaigns.clientId,c.ids),sql`${adCampaigns.id} in (select id from ad_campaigns where workspace_id=${c.workspaceId} and archived_at is not null)`))','GET archived workspace');

replace('const id=String(b.campaignId||""),[x]=await db.select().from(adCampaigns).where(eq(adCampaigns.id,id)).limit(1);','const id=String(b.campaignId||""),[x]=await db.select().from(adCampaigns).where(and(eq(adCampaigns.id,id),eq(adCampaigns.workspaceId,c.workspaceId))).limit(1);','archive read workspace');
replace('sql`select 1 as exists from ad_campaigns where id=${id} and archived_at is not null limit 1`','sql`select 1 as exists from ad_campaigns where id=${id} and workspace_id=${c.workspaceId} and archived_at is not null limit 1`','archive state workspace');
replace('sql`update ad_campaigns set archived_at=now(),archived_by=${c.userId},updated_at=now() where id=${id} and archived_at is null`','sql`update ad_campaigns set archived_at=now(),archived_by=${c.userId},updated_at=now() where id=${id} and workspace_id=${c.workspaceId} and archived_at is null`','archive update workspace');
replace('sql`update ad_campaigns set archived_at=null,archived_by=null,updated_at=now() where id=${id} and archived_at is not null`','sql`update ad_campaigns set archived_at=null,archived_by=null,updated_at=now() where id=${id} and workspace_id=${c.workspaceId} and archived_at is not null`','restore update workspace');
replace('await db.delete(adPerformanceDaily).where(eq(adPerformanceDaily.campaignId,id));await db.delete(adCampaigns).where(eq(adCampaigns.id,id))','await db.transaction(async tx=>{await tx.execute(sql`select pg_advisory_xact_lock(hashtext(${`media-v2-delete:${id}`}))`);await tx.delete(adPerformanceDaily).where(eq(adPerformanceDaily.campaignId,id));const deleted=await tx.delete(adCampaigns).where(and(eq(adCampaigns.id,id),eq(adCampaigns.workspaceId,c.workspaceId))).returning({id:adCampaigns.id});if(!deleted.length)throw new Error("Campaign state changed before deletion completed.")})','delete transaction');

replace('db.select().from(adPlatformConnections).where(eq(adPlatformConnections.id,connectionId)).limit(1)','db.select().from(adPlatformConnections).where(and(eq(adPlatformConnections.id,connectionId),eq(adPlatformConnections.workspaceId,c.workspaceId))).limit(1)','link connection workspace');
replace('const [existing]=await db.select().from(adCampaigns).where(and(eq(adCampaigns.platform,a.platform),eq(adCampaigns.externalId,externalId))).limit(1);if(existing&&existing.clientId!==clientId)return NextResponse.json({error:"That campaign is already linked to another client."},{status:409});','const [existing]=await db.select({id:adCampaigns.id,clientId:adCampaigns.clientId,workspaceId:adCampaigns.workspaceId}).from(adCampaigns).where(and(eq(adCampaigns.platform,a.platform),eq(adCampaigns.externalId,externalId))).limit(1);if(existing&&existing.workspaceId!==c.workspaceId)return NextResponse.json({error:"That campaign is already linked outside this workspace."},{status:409});if(existing&&existing.clientId!==clientId)return NextResponse.json({error:"That campaign is already linked to another client."},{status:409});','link collision workspace');

replace('db.select().from(adCampaigns).where(eq(adCampaigns.id,String(b.campaignId||""))).limit(1)','db.select().from(adCampaigns).where(and(eq(adCampaigns.id,String(b.campaignId||"")),eq(adCampaigns.workspaceId,c.workspaceId))).limit(1)','sync campaign workspace');
replace('sql`select 1 as exists from ad_campaigns where id=${campaign.id} and archived_at is null limit 1`','sql`select 1 as exists from ad_campaigns where id=${campaign.id} and workspace_id=${c.workspaceId} and archived_at is null limit 1`','sync active workspace');
replace('db.select().from(adPlatformConnections).where(eq(adPlatformConnections.id,campaign.connectionId)).limit(1)','db.select().from(adPlatformConnections).where(and(eq(adPlatformConnections.id,campaign.connectionId),eq(adPlatformConnections.workspaceId,c.workspaceId))).limit(1)','sync connection workspace');
replace('where id=${campaign.id}`','where id=${campaign.id} and workspace_id=${c.workspaceId}`','summary workspace');
replace('.where(eq(adCampaigns.id,campaign.id));await db.update(adPlatformConnections)', '.where(and(eq(adCampaigns.id,campaign.id),eq(adCampaigns.workspaceId,c.workspaceId)));await db.update(adPlatformConnections)','campaign update workspace');
replace('.where(eq(adPlatformConnections.id,a.id));const auditValue', '.where(and(eq(adPlatformConnections.id,a.id),eq(adPlatformConnections.workspaceId,c.workspaceId)));const auditValue','connection success workspace');
replace('.where(eq(adPlatformConnections.id,a.id));return NextResponse.json({error:message},{status:502})', '.where(and(eq(adPlatformConnections.id,a.id),eq(adPlatformConnections.workspaceId,c.workspaceId)));return NextResponse.json({error:message},{status:502})','connection error workspace');

fs.writeFileSync(path,s);
console.log("media-control-v2 hardening applied");