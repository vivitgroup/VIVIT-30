import fs from 'node:fs';
const file='app/api/assistant/actions/route.ts';let s=fs.readFileSync(file,'utf8');
s=s.replace('type AuditPayloadRow={id?:string;new_values:string|null};','type AuditPayloadRow={id?:string;action?:string;new_values:string|null};');
const start=' if(requestId){const [done]=Array.from(await db.execute<AuditPayloadRow>';
const end='\n try{const result=await execute(op,args,role,userId,workspaceId)';
const a=s.indexOf(start),b=s.indexOf(end,a);if(a<0||b<0)throw new Error('VIVITO idempotency anchor not found');
const claim=` let receiptId="";\n if(requestId){\n  receiptId=\`vivito:req:\${workspaceId}:\${userId}:\${requestId}\`;\n  const startedValues=JSON.stringify({requestId,op,args,state:"STARTED"});\n  const [claimed]=Array.from(await db.execute<{id:string}>(sql\`insert into audit_logs(id,workspace_id,user_id,action,entity,entity_id,new_values,created_at) values(\${receiptId},\${workspaceId},\${userId},'vivito_action_started','vivito',\${requestId},\${startedValues},now()) on conflict (id) do nothing returning id\`));\n  if(!claimed){\n   const [prior]=Array.from(await db.execute<AuditPayloadRow>(sql\`select id,action,new_values from audit_logs where id=\${receiptId} and workspace_id=\${workspaceId} and user_id=\${userId} limit 1\`));\n   const previous=parseRecord(prior?.new_values);\n   if(prior?.action==="vivito_action_executed")return NextResponse.json({success:true,duplicate:true,action:op,result:previous.result||null},{headers:noStore});\n   if(prior?.action==="vivito_action_failed")return NextResponse.json({error:previous.message||"The prior execution failed.",failureId:receiptId,requestId,duplicate:true,retryWith:"retryOf"},{status:Number(previous.status||409),headers:noStore});\n   return NextResponse.json({error:"A prior request with this requestId is still in progress or its outcome is uncertain. VIVITO will not execute it twice.",requestId,duplicate:true},{status:409,headers:noStore});\n  }\n }`;
s=s.slice(0,a)+claim+s.slice(b);
const oldTry=' try{const result=await execute(op,args,role,userId,workspaceId),external=await applyExternalCampaignWrite(op,args,result,userId,workspaceId),combined=external?{...result,external}:result;const auditValue:typeof auditLogs.$inferInsert={workspaceId,userId,action:"vivito_action_executed",entity:"vivito",entityId:String(result.entityId||requestId||crypto.randomUUID()),newValues:JSON.stringify({requestId:requestId||null,op,result:combined,approval:decideVivitoApproval(op,role)})};await db.insert(auditLogs).values(auditValue);return NextResponse.json({success:true,action:op,result:combined},{headers:noStore})}catch(error){const status=error instanceof VivitoActionError?error.status:500,message=error instanceof Error?error.message:"VIVITO could not execute the action safely.";const failureId=crypto.randomUUID(),auditValue:typeof auditLogs.$inferInsert={id:failureId,workspaceId,userId,action:"vivito_action_failed",entity:"vivito",entityId:requestId||failureId,newValues:JSON.stringify({requestId:requestId||null,op,args,message,status,details:error instanceof VivitoActionError?error.details||null:null})};await db.insert(auditLogs).values(auditValue).catch(()=>{});if(error instanceof VivitoActionError)return NextResponse.json({error:error.message,details:error.details||null,failureId},{status:error.status,headers:noStore});console.error("VIVITO action execution failed",error);return NextResponse.json({error:"VIVITO could not execute the action safely.",failureId},{status:500,headers:noStore})}';
const newTry=' try{const result=await execute(op,args,role,userId,workspaceId),external=await applyExternalCampaignWrite(op,args,result,userId,workspaceId),combined=external?{...result,external}:result,entityId=String(result.entityId||requestId||crypto.randomUUID()),successValues=JSON.stringify({requestId:requestId||null,op,result:combined,approval:decideVivitoApproval(op,role)});if(receiptId){const updated=Array.from(await db.execute(sql`update audit_logs set action=\'vivito_action_executed\',entity_id=${entityId},new_values=${successValues} where id=${receiptId} and workspace_id=${workspaceId} and user_id=${userId} and action=\'vivito_action_started\' returning id`));if(!updated.length)throw new VivitoActionError("VIVITO completed the action but could not finalize its execution receipt.",500,{outcomeUncertain:true,requestId})}else{const auditValue:typeof auditLogs.$inferInsert={workspaceId,userId,action:"vivito_action_executed",entity:"vivito",entityId,newValues:successValues};await db.insert(auditLogs).values(auditValue)}return NextResponse.json({success:true,action:op,result:combined},{headers:noStore})}catch(error){const status=error instanceof VivitoActionError?error.status:500,message=error instanceof Error?error.message:"VIVITO could not execute the action safely.",failureId=receiptId||crypto.randomUUID(),failureValues=JSON.stringify({requestId:requestId||null,op,args,message,status,details:error instanceof VivitoActionError?error.details||null:null});if(receiptId)await db.execute(sql`update audit_logs set action=\'vivito_action_failed\',entity_id=${failureId},new_values=${failureValues} where id=${receiptId} and workspace_id=${workspaceId} and user_id=${userId} and action=\'vivito_action_started\'`).catch(()=>{});else{const auditValue:typeof auditLogs.$inferInsert={id:failureId,workspaceId,userId,action:"vivito_action_failed",entity:"vivito",entityId:requestId||failureId,newValues:failureValues};await db.insert(auditLogs).values(auditValue).catch(()=>{})}if(error instanceof VivitoActionError)return NextResponse.json({error:error.message,details:error.details||null,failureId},{status:error.status,headers:noStore});console.error("VIVITO action execution failed",error);return NextResponse.json({error:"VIVITO could not execute the action safely.",failureId},{status:500,headers:noStore})}';
if(!s.includes(oldTry))throw new Error('VIVITO execution block not found');s=s.replace(oldTry,newTry);fs.writeFileSync(file,s);console.log('VIVITO idempotency receipt patch applied.');
