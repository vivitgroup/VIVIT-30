import fs from 'node:fs';
const checks=[];const add=(name,ok)=>checks.push({name,ok});const read=p=>fs.readFileSync(p,'utf8');
const header=read('components/layout/Header.tsx'),home=read('components/layout/WorkspaceHomeButton.tsx'),launcher=read('components/layout/OperatingSystemLauncher.tsx'),notices=read('components/realtime-notifications.tsx'),media=read('components/creative/TaskMediaPanel.tsx'),mobile=read('components/layout/MobileNav.tsx'),sidebar=read('components/layout/Sidebar.tsx');
add('Workspace home target is >=44px',/height:44px/.test(home)&&/min-width:44px/.test(home));
add('Apps launcher target is >=44px',/height:44px/.test(launcher)&&/min-width:44px/.test(launcher));
add('Apps launcher supports Escape and focus return',/e\.key==="Escape"/.test(launcher)&&/buttonRef\.current\?\.focus/.test(launcher));
add('Apps menu communicates current page',/aria-current=/.test(launcher));
add('Apps launcher honors mobile safe area',/safe-area-inset-bottom/.test(launcher));
add('Search closes consistently with focus return',/closeSearch/.test(header)&&/searchButtonRef\.current\?\.focus/.test(header)&&/e\.key\.toLowerCase\(\)==="k"/.test(header));
add('Search dialog uses dvh bounds',/80dvh/.test(header)&&/56dvh/.test(header));
add('Search results expose listbox semantics',/role="listbox"/.test(header)&&/role="option"/.test(header)&&/aria-selected/.test(header));
add('Header notifications target is 44px',/width:44px;height:44px/.test(header));
add('Realtime notification controls are >=44px',/width:44,height:44/.test(notices)&&/flex:"0 0 44px"/.test(notices));
add('Realtime notifications use live-region semantics',/aria-live="polite"/.test(notices)&&/role=\{urgent\?"alert":"status"\}/.test(notices));
add('Linked notifications are keyboard buttons',/Open notification/.test(notices)&&/<button type="button" onClick=\{\(\)=>openToast/.test(notices));
add('Notification stack clears mobile nav safe area',/bottom:calc\(88px \+ env\(safe-area-inset-bottom\)\)/.test(notices));
add('Notifications respect reduced motion',/prefers-reduced-motion:reduce/.test(notices));
add('Task media ratios have 44px targets',/\.tm-ratios button\{min-height:44px/.test(media));
add('Task media lightbox supports Escape and focus return',/e\.key==="Escape"/.test(media)&&/openerRef\.current\?\.focus/.test(media));
add('Task media lightbox locks background scroll',/document\.body\.style\.overflow="hidden"/.test(media));
add('Task media lightbox has dialog label',/aria-label=\{`Preview \$\{open\.name\}`\}/.test(media));
add('Task media progress is announced',/role="progressbar"/.test(media)&&/aria-valuenow=\{progress\}/.test(media));
add('Mobile navigation labels VIVITO consistently',/label:"VIVITO"/.test(mobile)&&!/label:"AI Assistant"/.test(mobile));
add('Mobile drawer hidden state is inert',/inert=\{!open\}/.test(mobile));
add('Sidebar active routes expose aria-current',/aria-current=/.test(sidebar));
add('Sidebar controls use explicit button types',/(type="button"[^>]*onClick=\{toggleTheme\}|onClick=\{toggleTheme\}[^>]*type="button")/.test(sidebar)&&/(type="button"[^>]*onClick=\{collapse\}|onClick=\{collapse\}[^>]*type="button")/.test(sidebar));
for(const c of checks)console.log(`${c.ok?'PASS':'FAIL'}  ${c.name}`);const pass=checks.filter(c=>c.ok).length;console.log(`\n${pass}/${checks.length} hardcore UI/UX interaction checks passed.`);if(pass!==checks.length)process.exit(1);
