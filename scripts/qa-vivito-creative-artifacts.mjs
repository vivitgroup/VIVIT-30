import fs from"node:fs";
const academy=fs.readFileSync("lib/vivito/academy.ts","utf8"),batch=fs.readFileSync("lib/vivito/training-batch-11-creative-ops.ts","utf8"),artifact=fs.readFileSync("lib/vivito/artifact-intelligence.ts","utf8"),pdf=fs.readFileSync("lib/vivito/pdf-renderer.ts","utf8"),xlsx=fs.readFileSync("lib/vivito/xlsx-renderer.ts","utf8"),multi=fs.readFileSync("lib/vivito/multimodal.ts","utf8"),pdfRoute=fs.readFileSync("app/api/assistant/artifacts/pdf/route.ts","utf8"),xlsxRoute=fs.readFileSync("app/api/assistant/artifacts/xlsx/route.ts","utf8"),visionRoute=fs.readFileSync("app/api/assistant/vision/route.ts","utf8"),researchRoute=fs.readFileSync("app/api/assistant/research/route.ts","utf8");
const c=[],$=(n,v)=>c.push([n,!!v]),lessons=(batch.match(/L\("/g)||[]).length;
$("Batch 11 has deep creative operations breadth",lessons>=38);
$("Batch 11 is wired into live academy",/VIVITO_TRAINING_BATCH_11_CONTEXT/.test(academy)&&/VIVITO_CREATIVE_OPS_DOCTRINE/.test(academy)&&/VIVITO_ARTIFACT_DOCTRINE/.test(academy));
$("Vision separates observation from inference",/Separate observation from inference/.test(batch)&&/low-confidence text recognition/.test(batch));
$("Vision protects supplied product and logo fidelity",/Protect reference fidelity/.test(batch)&&/No silent redesign/.test(batch));
$("Grounded research uses current Google Search tool",/google_search/.test(multi)&&/groundingMetadata/.test(multi)&&/webSearchQueries/.test(multi));
$("Research keeps a source hierarchy and claim ledger",/source hierarchy/i.test(batch)&&/claim ledger/i.test(batch)&&/No orphan statistics/.test(batch));
$("Design teaches hierarchy grid typography and color roles",/Read hierarchy before aesthetics/.test(batch)&&/reusable design system/.test(batch)&&/Typography/.test(batch)&&/Color has roles/.test(batch));
$("Content strategy is linked to business objectives",/business objective/.test(batch)&&/Build pillars/.test(batch)&&/Balance funnel roles/.test(batch));
$("Content plan rows are operational",/date, platform, pillar, format, objective, topic, hook, caption direction, CTA, KPI, owner and status/.test(batch));
$("Spreadsheet model separates inputs calculations outputs",/Separate inputs, calculations and outputs/.test(batch)&&/row grain/.test(batch));
$("Content plan converts to workbook",/contentPlanToWorkbook/.test(artifact)&&/Content Plan/.test(artifact)&&/Strategy/.test(artifact));
$("XLSX renderer emits real Office Open XML zip structures",/0x04034b50/.test(xlsx)&&/\[Content_Types\]\.xml/.test(xlsx)&&/xl\/workbook\.xml/.test(xlsx)&&/worksheets\/sheet/.test(xlsx));
$("XLSX route returns correct workbook MIME",/application\/vnd.openxmlformats-officedocument.spreadsheetml.sheet/.test(xlsxRoute)&&/contentPlanToWorkbook/.test(xlsxRoute));
$("Reels teaches 1-2 second hook and timestamped audiovisual beats",/first 1–2 seconds/.test(batch)&&/Script by beats and timestamps/.test(batch)&&/on-screen text/.test(batch));
$("Reels teaches one promise proof CTA and caption separation",/one core promise/.test(batch)&&/Proof beats claims/.test(batch)&&/CTA must match funnel stage/.test(batch)&&/Plan caption separately/.test(batch));
$("PDF renderer returns actual PDF binary syntax",/%PDF-1\.4/.test(pdf)&&/xref/.test(pdf)&&/\/Type \/Page/.test(pdf));
$("PDF renderer fails closed for unsupported Arabic",/Arabic\/RTL text requires an embedded Unicode font renderer/.test(pdf));
$("PDF route returns application pdf",/application\/pdf/.test(pdfRoute)&&/renderVivitoPdf/.test(pdfRoute));
$("Vision endpoint requires auth and real image bytes",/Unauthorized/.test(visionRoute)&&/base64/.test(visionRoute)&&/analyzeVivitoImage/.test(visionRoute));
$("Research endpoint requires auth and grounded adapter",/Unauthorized/.test(researchRoute)&&/groundedVivitoResearch/.test(researchRoute));
$("Multimodal adapter validates MIME and size",/unsupported-image-type/.test(multi)&&/12\*1024\*1024/.test(multi));
$("Artifact doctrine refuses fake execution",/actual image bytes/.test(batch)&&/actual PDF bytes/.test(batch)&&/actual tabular output/.test(batch));
const f=c.filter(x=>!x[1]);for(const[n,v]of c)console.log(`${v?"PASS":"FAIL"}  ${n}`);console.log(`\n${c.length-f.length}/${c.length} VIVITO Creative Artifact checks passed.`);if(f.length)process.exit(1);
