import fs from"node:fs";
const academy=fs.readFileSync("lib/vivito/academy.ts","utf8");
const batch=fs.readFileSync("lib/vivito/training-batch-09-deliverables.ts","utf8");
const blueprints=fs.readFileSync("lib/vivito/deliverable-blueprints.ts","utf8");
const c=[],$=(n,v)=>c.push([n,!!v]);
const lessons=(batch.match(/domain:\"/g)||[]).length;
const decks=(blueprints.match(/id:\"/g)||[]).length;
$("Batch 09 contains broad deliverable mastery",lessons>=18);
$("Blueprint library covers at least five deliverables",decks>=5);
$("Blueprints cover strategy marketing media research SWOT PESTEL",["strategy","marketing-plan","media-buying","market-research","swot-pestel"].every(x=>blueprints.includes(`id:\"${x}\"`)));
$("Market research teaches methodology and source hierarchy",/Research begins with a decision question/.test(batch)&&/source hierarchy/.test(batch)&&/Triangulate important claims/.test(batch));
$("Research ethics anchored to ESOMAR and Pew",/ICC\/ESOMAR International Code 2025/.test(batch)&&/Pew Research Center/.test(batch));
$("Survey research teaches neutral pretested questions",/clear, neutral and single-concept/.test(batch)&&/Pretest new questionnaires/.test(batch));
$("Competitor research forbids invented performance",/Do not infer financial performance from social activity/.test(batch)&&/No invented sales\/share/.test(batch));
$("PESTEL converts environment into implications",/impact, likelihood and time horizon/.test(batch)&&/Translate to opportunities\/threats/.test(batch));
$("SWOT teaches internal external distinction and TOWS",/Strengths\/weaknesses must be internal/.test(batch)&&/Use TOWS/.test(batch));
$("Marketing plan links business goals to customer channel budget KPI",/Start from business objectives/.test(batch)&&/STP before channels/.test(batch)&&/Budget follows strategy/.test(batch));
$("Media plan includes scenario forecast tracking and stop loss",/Forecasts are scenarios, not promises/.test(batch)&&/tracking must be validated/i.test(batch)&&/stop[- ]loss/i.test(blueprints+batch));
$("Strategic storytelling uses assertion titles",/assertion titles/.test(batch)&&/answer-first communication/.test(batch));
$("Slide design doctrine teaches hierarchy whitespace and brand discipline",/visual hierarchy/.test(blueprints)&&/whitespace/i.test(blueprints)&&/brand colors as accents/.test(blueprints));
$("Data visualization rejects decorative and deceptive charts",/Do not use 3D charts/.test(batch)&&/Do not manipulate axes deceptively/.test(batch));
$("Visual research evidence cannot be faked",/No fake documentary imagery used as research evidence/.test(batch));
$("PDF and presentation export require page by page QA",/PDF & Presentation Export QA/.test(batch)&&/Zero clipped text/.test(batch)&&/No missing assets/.test(batch));
$("Citation ledger forbids fabricated citations",/Do not fabricate citations/.test(batch)&&/No orphan claims/.test(batch));
$("Operating doctrine refuses false binary artifact claims",/never claim a PDF\/PPTX was generated/.test(batch)&&/actual binary artifact generation is unavailable/.test(batch));
$("Academy wires Batch 09 training into runtime",/VIVITO_TRAINING_BATCH_09_CONTEXT/.test(academy)&&/STRATEGY, RESEARCH & DELIVERABLE MASTERY — BATCH 09/.test(academy));
$("Academy wires blueprints and design doctrine into runtime",/VIVITO_DELIVERABLE_BLUEPRINTS_CONTEXT/.test(academy)&&/VIVITO_DELIVERABLE_DESIGN_DOCTRINE/.test(academy));
const f=c.filter(x=>!x[1]);for(const[n,v]of c)console.log(`${v?"PASS":"FAIL"}  ${n}`);console.log(`\n${c.length-f.length}/${c.length} VIVITO Deliverable Mastery checks passed.`);if(f.length)process.exit(1);
