import fs from "node:fs";
const batch=fs.readFileSync("lib/vivito/training-batch-13-mastery.ts","utf8");
const academy=fs.readFileSync("lib/vivito/academy.ts","utf8");
let p=0,f=0;const c=(n,o)=>{console.log(`${o?"PASS":"FAIL"}  ${n}`);if(o)p++;else f++};
const cases=(batch.match(/\bM\("/g)||[]).length;
c("Batch 13 contains at least 50 integrated mastery cases",cases>=50);
c("Batch 13 spans enterprise strategy finance marketing research media creative content sales account operations DBA artifacts competitive vision web and AI",["Enterprise Strategy","Managerial Finance","Marketing Strategy","Market Research","Media Buying","Creative Strategy","Content Strategy","Sales Diagnosis","Account Management","Operations","Research Design","Executive PDF","Executive Presentation","Spreadsheet Modeling","Competitive Intelligence","Vision & Design QA","Web Research","AI Judgment"].every(x=>batch.includes(`\"${x}\"`)));
c("Batch 13 teaches enterprise economics and cash discipline",/contribution/.test(batch)&&/cash flow/.test(batch)&&/Working capital/.test(batch)&&/payback/.test(batch));
c("Batch 13 teaches STP positioning GTM and customer strategy",/Segmentation/.test(batch)&&/Positioning/.test(batch)&&/Go-To-Market/.test(batch)&&/value proposition/.test(batch));
c("Batch 13 teaches rigorous market and causal research",/top-down and bottom-up/.test(batch)&&/Sampling/.test(batch)&&/Causal Inference/.test(batch)&&/counterfactual/.test(batch));
c("Batch 13 teaches media diagnosis beyond vanity metrics",/CTR measures click response/.test(batch)&&/Platform attribution and commercial truth/.test(batch)&&/marginal expected value/.test(batch));
c("Batch 13 teaches design systems visual analysis and creative refresh",/Hierarchy before decoration/.test(batch)&&/Systems scale quality/.test(batch)&&/winning mechanism/.test(batch));
c("Batch 13 teaches operational content plans and Reels scripting",/Every row must have a job/.test(batch)&&/timed beats/.test(batch)&&/shot, VO, on-screen text and edit cues/.test(batch));
c("Batch 13 teaches sales and account judgment",/Lead volume is not pipeline quality/.test(batch)&&/No zombie pipeline/.test(batch)&&/scope creep/i.test(batch));
c("Batch 13 teaches safe multi-step ERP execution",/idempotency/.test(batch)&&/stop on first failure/.test(batch)&&/High\/destructive plans require confirmation/.test(batch));
c("Batch 13 teaches DBA evidence appraisal and managerial significance",/Evidence quality depends/.test(batch)&&/Statistical significance does not equal commercial relevance/.test(batch)&&/Mixed Methods/.test(batch));
c("Batch 13 teaches professional PDF presentation and spreadsheet production",/Zero clipping/.test(batch)&&/Slides are spoken visual arguments/.test(batch)&&/Separate inputs, calculations, outputs and assumptions/.test(batch));
c("Batch 13 deepens competitive intelligence and missing-data discipline",/Snapshots create proprietary history/.test(batch)&&/Missing data is not zero/.test(batch)&&/Recommend experiments, not copying/.test(batch));
c("Batch 13 teaches current-web evidence discipline and self-correction",/Freshness, authority and geographic relevance/.test(batch)&&/Correction is a strength/.test(batch)&&/Calibrate confidence/.test(batch));
c("Mastery doctrine frames, quantifies, researches, designs, self-corrects and fails closed",["FRAME BEFORE SOLVING","THINK CROSS-FUNCTIONALLY","RESEARCH BEFORE CLAIMING","QUANTIFY ECONOMICS","DESIGN FOR DECISION","FAIL CLOSED","SELF-CORRECT"].every(x=>batch.includes(x)));
c("Academy wires Batch 13 into runtime context",/VIVITO_TRAINING_BATCH_13_CONTEXT/.test(academy)&&/VIVITO_MASTERY_DOCTRINE/.test(academy)&&/INTEGRATED MASTER MINDSET — BATCH 13/.test(academy));
console.log(`\n${p}/${p+f} VIVITO Mastery Batch 13 checks passed.`);if(f)process.exit(1);