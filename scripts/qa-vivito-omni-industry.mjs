import fs from 'node:fs';
const batch=fs.readFileSync('lib/vivito/training-batch-16-omni-industry.ts','utf8');
const academy=fs.readFileSync('lib/vivito/academy.ts','utf8');
let p=0,f=0;const c=(n,o)=>{if(o){console.log('PASS '+n);p++}else{console.error('FAIL '+n);f++}};
const domains=[
'Hospitality & F&B','Ecommerce & DTC','B2B & Enterprise','Luxury & Premium','Automotive','Education','Beauty & Cosmetics','SaaS & Tech','Brand Strategy','Behavioral Economics','Pricing Science','Advanced Analytics','Experimentation','SEO, AEO & AI Search','Influencer & Creator','PR & Crisis','Customer Experience & Retention','GCC & International','RFP, Tenders & Proposals','Marketing Finance','Leadership & Agency Management','Integrated Executive Synthesis'];
c('covers all omni-industry domains',domains.every(x=>batch.includes(`M(\"${x}\"`)));
c('contains at least eighty modules',(batch.match(/\bM\(\"/g)||[]).length>=80);
c('hospitality economics',/RevPAR/.test(batch)&&/menu engineering/i.test(batch)&&/delivery-app economics/.test(batch));
c('ecommerce contribution economics',/COGS/.test(batch)&&/contribution LTV/.test(batch)&&/inventory/.test(batch));
c('B2B ABM and pipeline',/ABM/.test(batch)&&/buying committee/.test(batch)&&/pipeline/.test(batch));
c('luxury strategy',/scarcity/.test(batch)&&/brand codes/.test(batch)&&/high-ticket/.test(batch));
c('automotive funnels',/test drive/.test(batch)&&/aftersales/.test(batch)&&/PPF/.test(batch));
c('education economics',/completion/.test(batch)&&/cohort P&L/.test(batch));
c('beauty claims and retention',/Claims must match substantiation/.test(batch)&&/replenishment/.test(batch));
c('SaaS metrics',/MRR\/ARR/.test(batch)&&/CAC payback/.test(batch)&&/NRR/.test(batch));
c('brand strategy',/Distinctive assets/.test(batch)&&/Brand tracking/.test(batch)&&/Architecture/.test(batch));
c('ethical behavioral economics',/dark patterns/.test(batch)&&/fabricated scarcity/.test(batch));
c('pricing science',/Elasticity/.test(batch)&&/conjoint/.test(batch)&&/Willingness to pay/.test(batch));
c('advanced analytics',/MMM/.test(batch)&&/Cohorts/.test(batch)&&/Forecasting/.test(batch)&&/Anomaly/.test(batch));
c('experimentation rigor',/A\/B testing/.test(batch)&&/Holdouts/.test(batch)&&/Geo experiments/.test(batch)&&/statistically detectable/.test(batch));
c('SEO AEO AI search',/Technical SEO/.test(batch)&&/Topical authority/.test(batch)&&/AI answer visibility/.test(batch));
c('creator economics and rights',/creator scorecard/.test(batch)&&/Fraud/.test(batch)&&/usage period/.test(batch));
c('PR crisis',/Holding statement/.test(batch)&&/crisis matrix/.test(batch)&&/trust recovery/.test(batch));
c('retention and CX',/Churn/.test(batch)&&/NPS\/CSAT/.test(batch)&&/Service recovery/.test(batch));
c('GCC international',/Market entry/.test(batch)&&/Saudi\/UAE/.test(batch)&&/Cross-border/.test(batch));
c('RFP tender proposal',/RFP compliance matrix/.test(batch)&&/Win strategy/.test(batch)&&/Commercial response/.test(batch));
c('marketing finance',/P&L linkage/.test(batch)&&/client profitability/.test(batch)&&/Scenario planning/.test(batch));
c('leadership agency management',/Capacity planning/.test(batch)&&/Hiring/.test(batch)&&/Agency operating system/.test(batch));
c('cross-industry transfer safety',/Transfer principles, not tactics/.test(batch)&&/structural similarity/.test(batch));
c('evidence discipline',/FACT \/ INFERENCE \/ ASSUMPTION \/ RECOMMENDATION/.test(batch)&&/authoritative sources/.test(batch));
c('decision quality',/expected value/.test(batch)&&/reversibility/.test(batch)&&/confidence/.test(batch));
c('artifact quality',/content, evidence, math, visual, narrative and usability QA/.test(batch));
c('academy runtime wiring',/VIVITO_TRAINING_BATCH_16_CONTEXT/.test(academy)&&/OMNI-INDUSTRY BUSINESS & MARKETING MASTERY — BATCH 16/.test(academy));
console.log(`\n${p}/${p+f} omni-industry mastery checks passed.`);if(f)process.exit(1);
