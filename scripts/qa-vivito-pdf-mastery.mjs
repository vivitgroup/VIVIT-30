import fs from"node:fs";
const academy=fs.readFileSync("lib/vivito/academy.ts","utf8");
const batch=fs.readFileSync("lib/vivito/training-batch-10-pdf-mastery.ts","utf8");
const c=[],$=(n,v)=>c.push([n,!!v]);
const rules=(batch.match(/domain:\"/g)||[]).length;
$("Batch 10 contains deep PDF production mastery",rules>=40);
$("PDF mastery is wired into live academy runtime",/VIVITO_TRAINING_BATCH_10_CONTEXT/.test(academy)&&/VIVITO_PDF_MASTER_DOCTRINE/.test(academy)&&/EXECUTIVE PDF DESIGN & PRODUCTION MASTERY — BATCH 10/.test(academy));
$("PDF mastery teaches narrative architecture and assertion titles",/Narrative Architecture/.test(batch)&&/assertion-style headlines/.test(batch)&&/One page should communicate one dominant message/.test(batch));
$("PDF mastery teaches grid typography hierarchy whitespace",/Grid System/.test(batch)&&/Typography/.test(batch)&&/Visual Hierarchy/.test(batch)&&/Whitespace/.test(batch));
$("PDF mastery handles Arabic and bilingual layout",/Arabic Typography/.test(batch)&&/Bilingual Layout/.test(batch)&&/RTL/.test(batch)&&/glyph/.test(batch));
$("PDF mastery covers charts tables matrices frameworks",/Charts/.test(batch)&&/Tables/.test(batch)&&/Matrices/.test(batch)&&/SWOT \/ PESTEL Pages/.test(batch));
$("PDF mastery preserves vectors and image quality",/Vector Discipline/.test(batch)&&/Image Quality/.test(batch)&&/No blurry logos/.test(batch));
$("PDF mastery enforces numerical context and source traceability",/Every important number needs context/.test(batch)&&/No fabricated citation/.test(batch)&&/Source matches claim/.test(batch));
$("PDF mastery includes methodology appendix navigation",/Methodology/.test(batch)&&/Appendix/.test(batch)&&/Navigation/.test(batch)&&/bookmarks/i.test(batch));
$("PDF mastery includes accessibility and PDF UA awareness",/Accessibility/.test(batch)&&/PDF\/UA Awareness/.test(batch)&&/tagged structure/.test(batch)&&/logical reading order/.test(batch));
$("PDF mastery includes metadata filenames and links",/Metadata/.test(batch)&&/Filename & Versioning/.test(batch)&&/Links/.test(batch));
$("PDF mastery separates screen first and print first export",/Screen-first Export/.test(batch)&&/Print-first Export/.test(batch)&&/bleed/.test(batch)&&/CMYK/.test(batch));
$("PDF mastery covers compression and font embedding",/Compression/.test(batch)&&/Font Embedding/.test(batch)&&/No font substitution/.test(batch));
$("PDF mastery requires zero clipping and rendered-file inspection",/Zero clipped text/.test(batch)&&/Visual QA/.test(batch)&&/Inspect the rendered PDF/.test(batch));
$("PDF mastery audits consistency content visual narrative",/Consistency QA/.test(batch)&&/Content QA/.test(batch)&&/Visual QA/.test(batch)&&/Narrative QA/.test(batch));
$("PDF mastery targets boardroom readiness",/Executive Standard/.test(batch)&&/boardroom-ready/.test(batch)&&/Next action is obvious/.test(batch));
$("PDF doctrine refuses false 100 percent claims",/Never claim 100% quality as a fact before rendered-file inspection/.test(batch));
$("PDF doctrine refuses false binary artifact claims",/Never claim a PDF binary exists unless an actual renderer produced a file/.test(batch));
$("PDF accessibility uses Adobe and PDF Association anchors",/Adobe PDF Accessibility/.test(batch)&&/PDF Association PDF\/UA/.test(batch)&&/ISO 14289/.test(batch));
const f=c.filter(x=>!x[1]);for(const[n,v]of c)console.log(`${v?"PASS":"FAIL"}  ${n}`);console.log(`\n${c.length-f.length}/${c.length} VIVITO PDF Mastery checks passed.`);if(f.length)process.exit(1);
