import fs from 'node:fs';
const batch=fs.readFileSync('lib/vivito/training-batch-15-real-estate-marketing.ts','utf8');
const academy=fs.readFileSync('lib/vivito/academy.ts','utf8');
let p=0,f=0;const c=(n,o)=>{if(o){console.log('PASS  '+n);p++}else{console.error('FAIL  '+n);f++}};
const modules=(batch.match(/M\("/g)||[]).length;
c('Real estate batch has deep breadth',modules>=50);
c('Covers major asset classes',/residential sale/.test(batch)&&/commercial/.test(batch)&&/off-plan/.test(batch)&&/resale/.test(batch)&&/rental/.test(batch));
c('Geo-persona is evidence based',/Build persona FROM PLACE/.test(batch)&&/commute corridors/.test(batch)&&/POI cluster/.test(batch)&&/search demand/.test(batch));
c('Avoids demographic stereotyping from location',/never infer sensitive traits from location/i.test(batch)&&/avoid demographic stereotyping/i.test(batch));
c('Separates homebuyer investor tenant personas',/homebuyer/.test(batch)&&/investor/.test(batch)&&/tenant/.test(batch));
c('Covers persona economics',/budget band/.test(batch)&&/payment-plan sensitivity/.test(batch)&&/investment horizon/.test(batch));
c('Google Search mastery',/Google Search/.test(batch)&&/Keyword architecture/.test(batch)&&/negative/.test(batch));
c('Google Performance Max mastery',/Performance Max/.test(batch)&&/qualified\/closed outcomes/.test(batch)&&/conversion lag/.test(batch));
c('Google housing policy awareness',/US\/Canada/.test(batch)&&/ZIP-code/.test(batch)&&/Never propose workarounds/.test(batch));
c('Snapchat housing policy awareness',/housing ads/.test(batch)&&/age, gender or postal-code-level/.test(batch));
c('Snapchat audience and creative mastery',/Snapchat.*Audience strategy/s.test(batch)&&/Vertical, immediate first-frame clarity/.test(batch));
c('Meta real-estate strategy',/Meta/.test(batch)&&/Lead forms/.test(batch)&&/housing special-category restrictions/.test(batch));
c('TikTok YouTube LinkedIn portals covered',/TikTok/.test(batch)&&/YouTube/.test(batch)&&/LinkedIn/.test(batch)&&/Property portal strategy/.test(batch));
c('Tracks downstream funnel economics',/qualified lead/.test(batch)&&/viewing/.test(batch)&&/reservation/.test(batch)&&/contract/.test(batch)&&/revenue/.test(batch));
c('Optimizes beyond CPL',/not cheap CPL alone/.test(batch)&&/higher CPL can be superior/.test(batch));
c('CRM and offline conversions covered',/offline conversion/.test(batch)&&/CRM/.test(batch));
c('Cohort and sales-cycle analysis',/lead-date cohorts/.test(batch)&&/sales cycle/.test(batch));
c('Investment modeling is rigorous',/gross yield/.test(batch)&&/net yield/.test(batch)&&/vacancy/.test(batch)&&/exit liquidity/.test(batch));
c('Creative covers maps floorplans progress scarcity',/Location-led concept/.test(batch)&&/Floor plans/.test(batch)&&/Construction progress/.test(batch)&&/Scarcity/.test(batch));
c('Claims governance blocks invented returns',/Never guarantee ROI/.test(batch)&&/Every numeric project claim needs owner\/source\/date/.test(batch));
c('Content and Reels covered',/Content pillars/.test(batch)&&/Property reel/.test(batch)&&/Neighborhood guide/.test(batch));
c('Competitor intelligence covered',/Five competing projects/.test(batch)&&/price\/payment shifts/.test(batch));
c('SWOT and PESTEL covered',/SWOT\/PESTEL/.test(batch)&&/interest rates\/credit/.test(batch));
c('Sales and broker strategy covered',/Sales Enablement/.test(batch)&&/Broker Strategy/.test(batch));
c('Forecasting uses scenarios',/base\/upside\/downside/.test(batch));
c('Excel artifact structure covered',/assumptions, media budget, channel forecast, funnel, inventory, CRM cohort, geo analysis/.test(batch));
c('Self correction updates persona from evidence',/New evidence contradicts persona/.test(batch)&&/Update the persona/.test(batch));
c('Batch wired into runtime academy',/VIVITO_TRAINING_BATCH_15_CONTEXT/.test(academy)&&/REAL ESTATE MARKETING MASTERY — BATCH 15/.test(academy));
console.log(`\n${p}/${p+f} Real Estate Marketing checks passed.`);if(f)process.exit(1);
